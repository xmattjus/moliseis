> Tasks 0–10 record the original Subplan 4 work completed in `d61dadd2091c6924587dbc8d9d0dd64a91dd6fc5`. Tasks 11–14 record the completed focused remediation represented by the current implementation. Where an earlier checked task names generic `clear.execute()` finalization, the identity-bound mechanics in Tasks 12–13 supersede only that unsafe invocation while preserving the completed behavior.

## 0. Revalidate the implementation baseline

- [x] 0.1 Immediately before source edits, run `git status --short`, `git rev-parse HEAD`, `git merge-base --is-ancestor 9165a8a47fd08d4c87a33fa08b8d8b3d4cc473fc HEAD`, and a focused diff/log review; proceed only when HEAD contains authoritative synchronized baseline `9165a8a47fd08d4c87a33fa08b8d8b3d4cc473fc` as an ancestor or is an explicitly reviewed equivalent state with the completed predecessor capabilities already synchronized into `openspec/specs/`, preserve unrelated work, and stop for an OpenSpec update if later production changes overlap checkpointing, staging, submit, finalization, DTO, or Edge-parser boundaries.
- [x] 0.2 Verify the completed `harden-content-submission-draft-persistence`, `harden-content-submission-local-asset-staging`, and `harden-content-submission-exit-progress-route` delta specs still match their files under `openspec/specs/`; confirm the main draft-persistence spec contains `Each logical local submission has a stable client identity`, and synchronize a changed predecessor before proceeding rather than weakening this change's MODIFIED delta.
- [x] 0.3 Reinspect `submission_validation.ts` and verify unknown top-level fields are still accepted; if `client_submission_id` would now be rejected, stop and update this OpenSpec instead of changing production backend code under this plan.
- [x] 0.4 Record the actual pre-change outputs for `flutter test test/ui/content_submission/view_models/content_submission_view_model_test.dart test/ui/content_submission/widgets/content_submission_screen_test.dart test/ui/content_submission/widgets/content_submission_progress_screen_test.dart test/routing/content_submission_route_test.dart test/routing/content_submission_restoration_test.dart`, `deno test supabase/functions/submit-content/submission_validation_test.ts`, and `flutter analyze`; retain diagnostics for later comparison and do not reuse historical test counts.
- [x] 0.5 Before the original `d61dadd` implementation, run `openspec validate harden-content-submission-submit-orchestration --strict` and verify the proposal, the then-existing draft-persistence and submit-orchestration delta files, design, and tasks are structurally valid; Task 11.4 supersedes this artifact-count check for remediation.

## 1. Rename and instrument the public submission contract

- [x] 1.1 Change `ContentSubmissionRepository` final whole-submission from positional `upload(...)` to named `submit({required clientSubmissionId, required contentSubmission, required submissionAssets})`, keep `Future<Result<void>>`, `uploadImageTask(File)`, and `dispose()` unchanged, and verify a repository-wide symbol search finds no production implementation or caller that requires an `upload` compatibility alias.
- [x] 1.2 Update `FakeContentSubmissionRepository` to expose submission-oriented result/counter/capture names and defensively capture call count, exact client identity, submitted `ContentSubmission`, and an immutable/non-growable copy of ordered `SubmissionAsset` arguments; verify focused fake consumers can assert historical arguments without later list mutation changing them.
- [x] 1.3 Update `ControllableSubmissionRepository` to gate `submit(...)`, capture its named arguments, and rename final-operation helpers such as `completeUpload` to submission terminology while leaving actual image-upload names unchanged; verify existing widget tests can still hold `submit` Command state pending and complete it with either `Result` branch.
- [x] 1.4 Mechanically migrate every implementation, caller, fixture, and test assertion from the old final `upload` contract to `submit`, without renaming Cloudinary tasks or changing navigation expectations; verify repository-wide search leaves no stale production whole-submission call or implementation.

## 2. Introduce the explicit public wire mapper

- [x] 2.1 Repeat repository-wide references for `ContentSubmissionDto`, `ContentSubmissionMapper`, `toDto(userId:`, and final serialization; verify whether the planning-baseline finding—only the public repository and mapper test consume them—still holds, and stop before deletion if a new production consumer exists.
- [x] 2.2 Add `lib/data/mappers/content_submission_wire_mapper.dart` and its barrel export as one top-level allowlisted mapper accepting client identity, immutable `ContentSubmission`, and ordered assets; verify its output has exactly `client_submission_id`, `category`, `city`, `name`, `description`, `description_delta`, `latitude`, `longitude`, `address`, `start_date`, `end_date`, `user_email`, `user_name`, and `assets` as top-level keys.
- [x] 2.3 Map non-null event instants through UTC ISO-8601, preserve nulls and the already-frozen JSON-compatible Delta, and reuse `SubmissionAssetMapper` for ordered `url`, dimensions, nullable `mime_type`, and nullable `duration_seconds`; verify focused mapper tests cover non-UTC inputs, null optionals, structural Delta preservation, nested order, and absence of `user_id`, timestamps, accepted terms, ObjectBox IDs, and checkpoint state.
- [x] 2.4 If Task 2.1 confirms no remaining production consumer, remove `content_submission_dto.dart`, `content_submission_mapper.dart`, their obsolete focused test, generated `content_submission_dto.mapper.dart`, and stale exports as change-caused cleanup; first try a verified `--build-filter` scoped to the deleted mapper output, fall back to `dart run build_runner build --delete-conflicting-outputs` only if necessary, and verify the final diff contains no unrelated Envied, ObjectBox, dart_mappable, dependency, or lockfile drift and no hand-edited generated code. If a verified production consumer remains, keep the DTO/mapper and verify no unrelated DTO refactor is introduced.

## 3. Harden ContentSubmissionRepositoryImpl

- [x] 3.1 Change `ContentSubmissionRepositoryImpl` to depend on `SupabaseClient` and inject `supabase.client` from `lib/config/dependencies.dart`; remove `currentUser?.id` and the `UserIdFetchFailed` preflight used by the old DTO, then verify final submission can construct a request without a locally populated auth user while the real Supabase client still owns bearer authentication.
- [x] 3.2 Implement `submit(...)` with the explicit wire mapper and exactly one `functions.invoke('submit-content', body: ...)`; verify its source and focused HTTP test contain no direct write to `content_submissions`, `submissions_assets`, or rate-limit tables and no automatic retry branch.
- [x] 3.3 Add a focused public `ContentSubmissionApiException` carrying status code, optional trimmed backend code, and message; when `FunctionException.details` is a map, independently preserve non-empty `code` and use non-empty `message`, otherwise use non-empty string `details`, then non-empty `reasonPhrase`, then stable `Content Submission request failed.`, always preserve HTTP status, log once with existing failure logging, and verify map, string, reason-phrase, empty-detail, and fallback tests without extracting a shared admin/public normalizer.
- [x] 3.4 Parse every non-throwing response as an object requiring `submission_id is int && submission_id > 0`, tolerate additional keys, return `Result.error(FormatException(...))` for every other shape, and continue returning `Result.success(null)` after valid acknowledgement; verify object/non-object, missing, zero, negative, double, string, positive, and positive-plus-metadata cases.
- [x] 3.5 Preserve generic `Exception` catch/log/result behavior and Sentry transaction finalization without extracting the admin normalizer or refactoring telemetry; verify a transport/client exception is returned after one request and observable repository failure logging occurs once without asserting Sentry internals or logging request PII.
- [x] 3.6 Add `test/data/repositories/content_submission_repository_impl_test.dart` using `RecordingSupabaseFunctionsHttpClient` and a real test `SupabaseClient`, with only the minimal Cloudinary dependency stub needed by the constructor; verify one POST reaches `/functions/v1/submit-content`, carries the supplied identity and exact complete body/order, uses the test client authorization path without `currentUser`, and covers all success/error contracts from Tasks 3.2–3.5.

## 4. Persist an explicitly captured draft snapshot

- [x] 4.1 Refactor the private checkpoint primitive to accept a supplied immutable `ContentSubmissionDraft`, and keep `_checkpointDraftInsideBoundary()` as the current-state delegating wrapper used by existing callers; verify public `checkpointDraft()` and picker/external-boundary behavior remain unchanged in existing tests.
- [x] 4.2 Preserve `unknown` persisted-state rejection and restored/equal no-op behavior, save exactly the supplied snapshot when required, advance `_checkpointedDraft` only to a successfully saved supplied snapshot, and never replace it on failure; verify focused tests cover unknown ownership, clean no-op, failed save, and exact argument identity/content.
- [x] 4.3 Add/retain a gated A→B checkpoint regression in which A is supplied, live state changes to B while save(A) is pending, and A succeeds; verify A becomes the baseline, B remains live and dirty, and no second persistence path or draft repository API is introduced.

## 5. Prepare one immutable attempt before remote work

- [x] 5.1 Add one private ViewModel-only submission-attempt record containing a captured draft and an unmodifiable ordered asset copy; verify no public/domain orchestration model, use case, queue, scheduler, or new package is added.
- [x] 5.2 Keep the `_submissionFinalizationPending` branch before new-attempt preparation so pending retries execute only existing local finalization; verify a pending retry creates no attempt, checkpoint, image task, or repository request.
- [x] 5.3 Implement one attempt-preparation operation inside the existing `_serialize` lifecycle queue that awaits established initialization, rejects unavailable staged state and over-capacity membership, captures draft and assets, evaluates the existing event-time persistence rule against that draft, and validates only `city`, `name`, `userEmail`, and `userName`; verify existing validation feedback remains semantically unchanged and no accepted-terms, form, category, geolocation, description-length, or new email policy is added.
- [x] 5.4 Checkpoint exactly the captured draft inside preparation and return an attempt only after success, then release `_serialize` before any Cloudinary task is created or network await begins; verify dirty asset and no-asset tests establish checkpoint completion before their respective first remote boundary, while a checkpoint error yields zero image tasks and zero final submit calls with unchanged identity/dirty semantics.
- [x] 5.5 Extend the existing successful Subplan 3 transition path test to verify the command's attempt preparation recognizes the unchanged already-durable snapshot and performs no redundant draft save before remote work.

## 6. Execute remote work only from the prepared attempt

- [x] 6.1 Iterate only the attempt's captured assets sequentially, create each `ImageUploadTask` when its turn begins, stop on the first task error, and append successful `SubmissionAsset` metadata in captured order; verify first-asset, later-asset, and ordered-success regressions and retain no rollback, deletion, parallel `Future.wait`, or cancellation coordination.
- [x] 6.2 Build immutable `ContentSubmission` only from the captured draft—category, city, name, text, Delta, UTC event instants, contributor email/name, and already-supported nullable domain values—and do not add location fields to `ContentSubmissionDraft`; verify a gated Cloudinary test mutates live fields after preparation but the final repository capture still matches the original draft.
- [x] 6.3 Invoke repository `submit` with the same captured draft's `clientSubmissionId`, the content derived from that draft, and ordered successful asset metadata; verify no post-preparation payload decision reads live form or asset membership and a controlled A-save/B-live race sends A's values/identity while B remains live and dirty after a forced remote error.
- [x] 6.4 Preserve retryable failure behavior: any Cloudinary error prevents final submit, any final repository error prevents local retirement, and staged descriptors/source paths remain available; verify first/later Cloudinary failures and final-submit failure retain the identity and paths, retry-after-Cloudinary eventually submits the original session identity, and retry-after-final-error sends both attempts with that same identity.
- [x] 6.5 Preserve deterministic Cloudinary duplicate/reuse expectations and remove or correct any stale source comment that recommends rollback/parallelism contrary to this plan; verify existing duplicate/retry tests remain semantically unchanged rather than being rewritten as rollback tests.

## 7. Preserve successful local finalization

- [x] 7.1 After valid repository success, establish the immediate `_submissionFinalizationPending = true` transition and ViewModel-owned finalization in `d61dadd`; repository errors and malformed acknowledgements return before finalization begins and no persistent success marker or progress-screen cleanup policy was added. The generic `clear.execute()` invocation used by that commit is superseded by the identity-bound private finalization in Tasks 12.3–13.2.
- [x] 7.2 Extend the remote-success/local-clear-failure regression through one or more retries; verify final repository `submit` is called exactly once, retries perform no preparation/checkpoint/Cloudinary work, the old identity stays current while pending, and one successful clear creates a different valid fresh identity exactly once.
- [x] 7.3 Keep existing persistence-first clear and best-effort staged-cleanup semantics unchanged; verify Subplan 3 progress/navigation finalization tests still pass without claiming stronger staged-cleanup error propagation than the current contract.

## 8. Maintain widget, routing, and restoration regressions

- [x] 8.1 Update Content Submission form/progress widget tests and production-router/restoration tests for renamed fake fields/helpers and the explicit submit arguments only; run their focused files and verify validation ordering, one progress push, running-state back blocking, remote-failure preservation, finalization-only retry, navigation-only success actions, and restored-idle recovery retain existing expectations.
- [x] 8.2 Run the complete existing `ContentSubmissionViewModel` suite after new regressions and verify every Subplan 1–3 checkpoint, ownership, staging, path, retry, clear, and identity assertion still passes without being weakened to accommodate the refactor.

## 9. Freeze current Edge compatibility

- [x] 9.1 Add one case to `supabase/functions/submit-content/submission_validation_test.ts` using `validSubmission()` plus a canonical UUID-v4-compatible `client_submission_id`; run `deno test supabase/functions/submit-content/submission_validation_test.ts` and verify parsing succeeds without asserting that `ValidatedContentSubmission` exposes or persists the field.
- [x] 9.2 Run `deno fmt --check supabase/functions/submit-content/submission_validation_test.ts` and inspect the backend diff; verify there are no changes to `submission_validation.ts`, `ValidatedContentSubmission`, `index.ts`, database types, RPCs, migrations, RLS, rate limits, or any other production backend source.

## 10. Verify the complete change

- [x] 10.1 Run Dart formatting on every intentionally changed authored Dart file and `deno fmt --check` on the changed Deno test; verify formatter checks are clean and generated Dart was not formatted or edited manually.
- [x] 10.2 Run focused mapper and `ContentSubmissionRepositoryImpl` tests, then `flutter test test/ui/content_submission/view_models/content_submission_view_model_test.dart`; verify all request, acknowledgement, error, checkpoint, race, order, identity, retry, and finalization cases pass before broader tests.
- [x] 10.3 Run the affected form/progress/routing/restoration files from Task 0.4 and the Deno command from Task 9.1; verify no Subplan 3 transition, navigation, restoration, or Edge compatibility regression.
- [x] 10.4 Run targeted Flutter analysis for all authored/touched Dart source and tests and require zero new in-scope diagnostics, then run repository-wide `flutter analyze` and compare diagnostic tuples with the recorded pre-change baseline; verify no new or worsened diagnostics without broadening work into unrelated lint cleanup.
- [x] 10.5 Run the complete `flutter test` suite because the domain interface and shared fakes are cross-cutting compile boundaries; verify a zero exit and investigate rather than weakening unrelated-looking failures that exercise the renamed contract.
- [x] 10.6 Run `openspec validate harden-content-submission-submit-orchestration --strict` and independently review implementation evidence against every ADDED/MODIFIED requirement and scenario; verify planning intent remains correct, update artifacts only if intent changed, and repair implementation rather than rewriting correct requirements around defects.
- [x] 10.7 Inspect the final diff and repository-wide references; verify there are no stale production whole-submission `upload(...)` calls, unintended SQL/RLS/RPC/production Edge/Cloudinary/admin/routing redesign, packages, generated noise, secrets, or payload PII in logs, and verify the request includes `client_submission_id` while no code or report claims database consumption or deduplication.
- [x] 10.8 Report completion only as **Subplan 4 complete / ready for the next hardening subplan**, explicitly document that ambiguous remote acknowledgement, server-side idempotency, and the existing best-effort staged-cleanup limitation remain open, and verify the report does not declare Content Submission globally release-proof.

## 11. Completed focused remediation baseline verification

- [x] 11.1 Immediately before remediation source edits, run `git status --short`, `git rev-parse HEAD`, and `git merge-base --is-ancestor d61dadd2091c6924587dbc8d9d0dd64a91dd6fc5 HEAD`; proceed only from `d61dadd` or a reviewed descendant preserving its immutable-attempt implementation, retain unrelated work, and stop for another OpenSpec update if ownership, checkpoint, clear, staging, or finalization code has changed materially.
- [x] 11.2 Reinspected the pre-remediation `ContentSubmissionViewModel`, `Command`, shared Content Submission fakes, focused ViewModel tests, and the three synchronized predecessor specs; verified `submit` and `clear` had only independent same-Command running guards and finalization invoked generic clear before the completed identity-bound remediation superseded it, and that all three `MODIFIED Requirement` headers resolved exactly.
- [x] 11.3 Record focused pre-remediation results for `flutter test test/ui/content_submission/view_models/content_submission_view_model_test.dart`, the affected progress/form/routing tests, and targeted plus repository-wide `flutter analyze`; compare later results to this checkout rather than reusing the original Subplan 4 report.
- [x] 11.4 Run `openspec validate harden-content-submission-submit-orchestration --strict`; confirm the three `MODIFIED` requirement files are complete replacement blocks and the new submit-orchestration `ADDED` requirements remain valid before source changes begin.

## 12. Completed minimal session ownership and guarded retirement

- [x] 12.1 Add one private nullable active-submission client-identity field to `ContentSubmissionViewModel`; inside the existing serialized preparation turn, reject an unexpected existing owner, checkpoint captured A, claim A immediately after checkpoint success and before returning the prepared attempt, and verify a gated preparation test leaves no interval in which a queued clear can retire A after remote work is authorized.
- [x] 12.2 Guard the public explicit-clear path inside `_serialize`: when an active submission owner exists, return a local error before `clearDraft`, staged-session cleanup, state reset, identity rotation, or replacement-session creation; preserve ordinary persistence-first clear/discard behavior unchanged when no owner exists and verify both branches with focused tests.
- [x] 12.3 Extract the existing persistence-first draft deletion, best-effort staged cleanup, in-memory reset, and fresh-identity creation into one private inside-boundary retirement primitive reused by guarded explicit clear and successful finalization; verify no cleanup logic, repository API, staged error-propagation rule, public service/use-case layer, or second lifecycle queue is introduced.
- [x] 12.4 Replace successful finalization's call to the generic public clear Command with a private serialized identity-bound path: pass attempt A's `clientSubmissionId` on the first post-acknowledgement call, reuse the retained active owner as the expected identity on finalization-only retry, and verify pending state, active owner, and current logical identity all match before invoking the shared retirement primitive; on mismatch return before draft clear, staged cleanup, or rotation and retain acknowledged ownership/finalization state, verified by focused source review plus public ordering regressions without adding a test-only mutation API.
- [x] 12.5 On successful identity-bound retirement, clear finalization-pending state and active ownership only after A has been retired and one fresh identity installed, within the same serialized turn; verify a concurrent rejected clear followed by acknowledgement produces exactly one A-to-fresh rotation and no intermediate session.

## 13. Completed ownership release and retention for remote outcomes

- [x] 13.1 Wrap all post-preparation work before valid acknowledgement in `try`/`finally` or equivalent structured cleanup; for Cloudinary `Result.error`, final repository `Result.error`, malformed acknowledgement, transport failure, and unexpected thrown `Exception` or non-`Exception` object, briefly reacquire `_serialize`, release only the matching attempt owner, preserve the original error result/Command reporting, and verify no path can strand or overwrite ownership.
- [x] 13.2 After a valid acknowledgement, set finalization-pending state and mark the flow acknowledged synchronously before any later await; retain A's ownership across local retirement failure or throw, and verify later `submit.execute()` performs identity-bound finalization only with no checkpoint, Cloudinary task, or final repository call.
- [x] 13.3 Preserve the existing captured-state execution unchanged: lifecycle serialization remains released during Cloudinary and Edge waits, uploads remain sequential, and remote payloads remain derived only from attempt A; verify no `Command` global behavior, route/progress UI, backend, Cloudinary rollback, or parallel-upload change is present.
- [x] 13.4 Implement the documented direct-caller A→B policy without forking: if A fails before acknowledgement, release ownership and retain same-identity B as current and dirty relative to checkpoint A; if A is acknowledged and local retirement succeeds, retire same-identity B with A and create one fresh empty identity without copying fields or transferring staged ownership.

## 14. Completed ownership regressions and scope verification

- [x] 14.1 Add a Cloudinary-gated ViewModel regression: while upload for A is pending, external `clear.execute()` returns an error and causes zero `clearDraft` calls, zero staged-session cleanup calls, no identity rotation, and no replacement session; after the upload fails, ownership releases and a later explicit clear succeeds normally.
- [x] 14.2 Add a controllable-final-repository regression with the same clear assertions while acknowledgement for A is pending; complete the repository with an error and verify A remains current, its identity is unchanged, ownership releases, and later explicit clear is allowed.
- [x] 14.3 Add a preparation-gated ordering regression in which clear queues behind A's checkpoint; after the checkpoint succeeds and ownership is claimed, verify the queued clear fails before destructive work and A proceeds to the first remote boundary.
- [x] 14.4 Extend test support minimally to throw before valid acknowledgement and add regressions for both an ordinary `Exception` and, where the existing Command contract distinguishes it, a non-`Exception` object at both Cloudinary and final repository boundaries; verify the submit Command reports an error, matching ownership releases, session state remains recoverable and unacknowledged, and explicit clear is not permanently blocked.
- [x] 14.5 Add a valid-acknowledgement/local-clear-failure regression proving owner A and finalization-pending state remain active, external clear remains blocked without another `clearDraft`, repeated submit performs local finalization only, no image/final request repeats, and later successful retirement rotates exactly once and releases ownership.
- [x] 14.6 Add an identity-bound finalization regression using controlled clear/acknowledgement ordering: attempt replacement while A owns the session, verify that clear performs no `clearDraft` or staged cleanup, then assert the client identity captured by repository `submit` and the eventual staged-session cleanup target are both A; do not add a public/test-only state-mutation API merely to manufacture an otherwise unreachable identity mismatch.
- [x] 14.7 Add paired late-mutation regressions: while A's remote boundary is pending mutate live fields to same-identity B; on A failure verify B survives and is dirty relative to A, and on A success verify the repository received A while B is intentionally retired, exactly one fresh empty identity is created, and no B clone/copy or staged ownership transfer occurs.
- [x] 14.8 Rerun every existing Subplan 4 ViewModel regression, especially checkpoint-before-remote, A→B payload fidelity, asset order, Cloudinary/final-error identity retry, staged-path preservation, valid acknowledgement, and finalization-only retry; do not weaken prior assertions to accommodate ownership.
- [x] 14.9 Run the affected Content Submission form/progress/routing/restoration tests and complete `flutter test`; verify the required `ContentSubmissionScreen` session-identity listener migration returns a successful submission to a visibly cleared form, the guarded production UI and navigation-only progress behavior remain unchanged, and the cross-cutting Commands/fakes still compile.
- [x] 14.10 Run Dart formatting and targeted analysis for intentionally changed Dart/test files, then repository-wide `flutter analyze` against Task 11.3; require zero new in-scope diagnostics without unrelated lint cleanup.
- [x] 14.11 Run strict OpenSpec validation and independently compare all four deltas with synchronized main specs; verify no requirement still demands both an unchanged client envelope and `client_submission_id`, no requirement still assigns cleanup to both progress and the ViewModel, and ownership leaves no choice to defer or clear the current session.
- [x] 14.12 Inspect the final diff and verify changes are limited to the focused ViewModel ownership/finalization implementation, the narrow `ContentSubmissionScreen` session-identity listener migration, existing test support, and regressions: no `Command` global change, route or progress redesign, unrelated UI change, package, SQL, schema, unique constraint, RLS, RPC, rate-limit, production Edge Function, Admin, Cloudinary rollback/deletion, parallel upload, new architecture layer, or server-idempotency claim.
- [x] 14.13 Report completion only as **Subplan 4 focused remediation complete / ready for the next hardening subplan**, retaining ambiguous remote acknowledgement, absent server-side idempotency, and best-effort staged-cleanup limitations as explicit open work.
