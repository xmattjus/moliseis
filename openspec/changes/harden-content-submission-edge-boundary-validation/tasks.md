## 1. Lock scope and demonstrate the gap

- [ ] 1.1 Run `git status --short`, `git rev-parse HEAD`, and `git merge-base --is-ancestor fe8a7e2d0f563031cc8e3231f9a3147ed0bd4868 HEAD`; record unrelated work without modifying it, and stop if the implementation checkout does not preserve the Subplan 5 production boundary described in `design.md`.
- [ ] 1.2 Extend `supabase/functions/submit-content/submission_validation_test.ts` with table-driven regressions for both fields omitted, both null, mixed omitted/null absence, latitude-number with longitude omitted/null, longitude-number with latitude omitted/null, inclusive `-90`/`90` and `-180`/`180` using valid counterparts, immediately outside each limit, string/boolean/object/array inputs for each field where useful, `NaN`/positive infinity/negative infinity at the direct parser boundary, and an ordinary pair preserved exactly. Run `deno test supabase/functions/submit-content/submission_validation_test.ts` before production edits and verify the new pairing/range cases expose the current acceptance gap while pre-existing cases remain unchanged.

## 2. Implement the public coordinate contract

- [ ] 2.1 In `supabase/functions/submit-content/submission_validation.ts`, replace only the independent optional-finite coordinate checks with localized parsing that maps omission/null to null, rejects non-number/non-finite/out-of-range values with the existing field-specific messages, accepts inclusive latitude `[-90, 90]` and longitude `[-180, 180]`, rejects exactly-one-numeric pairs with `latitude and longitude must be provided together`, and returns accepted numbers unchanged. Run the focused parser test command from Task 1.2 and require every new and existing case to pass.
- [ ] 2.2 Update the existing `submission_store_test.ts` fixture from its semantically invalid null/numeric coordinate pair to a valid pair while preserving its exact-RPC-forwarding assertion; add no store validation. Run `deno test supabase/functions/submit-content/submission_store_test.ts` and require all forwarding/outcome tests to pass.

## 3. Prove persistence isolation at the handler boundary

- [ ] 3.1 Extend `supabase/functions/submit-content/index_test.ts` with representative authenticated incomplete-pair and out-of-range requests, including a valid canonical `client_submission_id`; assert the exact HTTP 400 `VALIDATION_ERROR` response/message, `calls.stores == 0`, and `store.calls.length == 0`. Run `deno test supabase/functions/submit-content/index_test.ts` and verify no invalid coordinate constructs or invokes the privileged store and no created/replayed/rate-limit behavior regresses.
- [ ] 3.2 Review handler sequencing in `index.ts` and verify no production handler edit is necessary: authentication remains before body parsing, complete parsing remains before `createStore()`, and the only persistence path remains one `SubmissionStore.submit()` call. Confirm the implementation diff contains no second validation path in the handler, store, RPC, or Flutter caller.

## 4. Focused and broader verification

- [ ] 4.1 Run `deno fmt --check supabase/functions/submit-content/submission_validation.ts supabase/functions/submit-content/submission_validation_test.ts supabase/functions/submit-content/index_test.ts supabase/functions/submit-content/submission_store_test.ts` and require a zero exit with no unrelated formatting changes.
- [ ] 4.2 Run `deno check supabase/functions/submit-content/index.ts supabase/functions/submit-content/submission_validation_test.ts supabase/functions/submit-content/index_test.ts supabase/functions/submit-content/submission_store_test.ts` and require a zero exit.
- [ ] 4.3 Run `deno test supabase/functions/submit-content/submission_validation_test.ts supabase/functions/submit-content/index_test.ts supabase/functions/submit-content/submission_store_test.ts` and require the complete submit-content parser, handler, and store suite to pass together. Do not add database or Flutter test runs unless implementation unexpectedly changes those production surfaces; if it does, stop and reconcile this OpenSpec instead of broadening scope silently.
- [ ] 4.4 Run `openspec validate harden-content-submission-edge-boundary-validation --strict --no-interactive`, `git diff --check`, `git status --short`, and `git diff --name-only`; require zero validation/whitespace errors and verify implementation changes are limited to the new change artifacts plus `submission_validation.ts` and the three focused submit-content test files.

## 5. Final adversarial review

- [ ] 5.1 Re-read the proposal, delta spec, design, and tasks against the implementation and adversarially test boundary bypasses, every nullable-coordinate combination, exact inclusive limits, immediately out-of-range values, direct non-finite inputs, exact value preservation, zero store construction/calls on invalid payloads, and replay identities with invalid coordinates.
- [ ] 5.2 Inspect the final diff and references to confirm Subplan 5 authentication, bounded parsing, idempotency, quota, one-RPC atomic persistence, created/replayed/rate-limited responses, and asset rules remain unchanged; confirm there is no migration, database/RPC constraint, generated type, Flutter, Admin/import/promotion, package, generic validation framework, unknown-field, Content-Type, email, MIME, Cloudinary, normalization, navigation, or UI scope expansion.
- [ ] 5.3 Report completion only as **Content Submission Hardening Subplan 6 complete: `submit-content` Edge boundary and validation hardening**. Keep whole-program release readiness deferred to the complete hardening sequence and record any genuine remaining boundary limitation without redesigning it in this change.
