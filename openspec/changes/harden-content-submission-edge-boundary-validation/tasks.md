## 1. Reconfirm the implementation boundary and producer contract

- [x] 1.1 Before implementation, run `git status --short`, `git rev-parse HEAD`, and `git merge-base --is-ancestor fe8a7e2d0f563031cc8e3231f9a3147ed0bd4868 HEAD`; record unrelated work without modifying it, and stop if the checkout no longer preserves the Subplan 5 authenticate/parse-before-store/single-RPC boundary described in `design.md`.
- [x] 1.2 Reinspect the public-ID generator, preparation client/function, signed upload fields, Flutter `/image/upload` request/response handling, shared duplicate lookup, and existing fixtures. Establish realistic non-production constants for at least two correlated `content_submissions/<64-lowercase-hex>` public IDs and canonical versioned `https://res.cloudinary.com/<test-cloud>/image/upload/...<format>` URLs, one representing normal upload and one dedupe lookup; verify no production secret or literal production cloud name is placed in source/tests.

## 2. Add focused failing boundary regressions

- [x] 2.1 Extend `supabase/functions/submit-content/submission_validation_test.ts` with table-driven coordinate coverage for omitted/omitted, null/null, omitted/null, null/omitted, `(0, 0)`, ordinary Molise values, exact `-90`/`90` and `-180`/`180` limits with valid counterparts, all numeric/omitted-or-null partial pairs, immediately out-of-range values, string/boolean/object/list forms, direct `NaN`/positive infinity/negative infinity, and exact preservation without clamping/rounding/swapping/wrapping. Run `deno test supabase/functions/submit-content/submission_validation_test.ts` before production edits and verify the new pairing/range cases expose the current gap.
- [x] 2.2 In the same parser suite, add realistic canonical normal-upload and dedupe URLs plus rejected HTTP, hostname, configured-cloud-name, namespace, `video/upload`, `image/fetch`, malformed/uppercase/short hash, unversioned, extensionless, transformed, extra/repeated-segment, percent-encoded/path-normalized, query, fragment, userinfo, and explicit-port cases. Require structural failures to use `asset url is not valid`, accepted URLs to remain byte-for-byte unchanged, and the focused test run to demonstrate the current HTTPS-plus-host acceptance gap.
- [x] 2.3 Add duplicate/count regressions for `[A]`, `[A, B]`, `[A, A]`, five distinct assets, and six assets. Require `[A, A]` to produce `assets must not contain duplicates`, distinct order/values to be preserved, and six entries to retain the pre-existing `assets length is not valid` result before duplicate/per-asset validation.
- [x] 2.4 Extend `supabase/functions/submit-content/index_test.ts` with only representative authenticated requests for a partial coordinate pair, an out-of-range coordinate, a structurally foreign Cloudinary URL, a duplicate asset, and a reused `client_submission_id` carrying new invalid coordinates or assets. For each, assert HTTP 400, code `VALIDATION_ERROR`, the stable applicable message, `calls.stores == 0`, and zero store calls; run `deno test supabase/functions/submit-content/index_test.ts` and verify the current gaps fail before production edits while existing handler outcomes remain green.

## 3. Implement complete public validation

- [x] 3.1 In `supabase/functions/submit-content/submission_validation.ts`, implement localized coordinate parsing that maps omission/null to null, rejects non-number/non-finite/out-of-range values with existing field messages, accepts inclusive latitude `[-90, 90]` and longitude `[-180, 180]`, rejects exactly-one-numeric pairs with `latitude and longitude must be provided together`, and returns accepted numbers unchanged. Re-run Task 2.1 and require the full coordinate matrix to pass.
- [x] 3.2 In `supabase/functions/_shared/cloudinary.ts`, expose or minimally adapt the existing generic delivery-URL parsing needed to validate HTTPS, exact `res.cloudinary.com`, and the configured cloud-name segment without embedding Content Submission namespace/resource/delivery/uniqueness policy. Keep duplicate lookup's exact returned-`public_id` check and importer behavior unchanged; extend `_shared/cloudinary_test.ts` for the generic helper and run `deno test supabase/functions/_shared/cloudinary_test.ts`.
- [x] 3.3 Add the Cloudinary cloud name to `submit-content` handler/validation dependencies, load `CLOUDINARY_CLOUD_NAME` with the existing `requiredEnv` pattern in `createProductionDependencies`, inject a non-production value in handler/parser tests, and pass it to pure validation. Verify `submission_validation.ts` contains no `Deno.env`, privileged client construction, or network access.
- [x] 3.4 In the public `parseContentSubmission` asset path, compose the generic Cloudinary identity check with local exact `image/upload/v<positive-decimal>/content_submissions/<64-lowercase-hex>.<lowercase-alphanumeric-format>` validation; reject credentials, explicit ports, transformations, non-canonical/encoded/extra path forms, queries, and fragments; preserve accepted URLs; and extract the public ID only as local validation metadata. Do not tighten exported `parseSubmissionAsset` for Admin or add public policy to the store/RPC. Re-run Task 2.2.
- [x] 3.5 Retain the maximum-five check before per-asset work, track each extracted validated public ID in a local set, reject repetition with `assets must not contain duplicates`, and leave distinct persisted assets/order unchanged. Re-run Task 2.3 and confirm no URL-canonicalization framework or database uniqueness mechanism was added.
- [x] 3.6 Correct `supabase/functions/submit-content/submission_store_test.ts` so its manually constructed validated submission uses a valid coordinate pair and realistic structurally correlated asset URLs while preserving exact RPC-forwarding assertions; add no store validation. Run `deno test supabase/functions/submit-content/submission_store_test.ts`.

## 4. Prove producer-to-consumer compatibility

- [x] 4.1 Correct or supplement unrealistic fixtures in `supabase/functions/_shared/cloudinary_test.ts` and `supabase/functions/prepare-cloudinary-upload/index_test.ts` so each duplicate lookup public ID, returned `public_id`, and top-level `secure_url` describe the same configured cloud, `image/upload`, version, `content_submissions/<64-hex>` identity, and format. Preserve the tests' original operational purposes and do not treat derived/transformed URLs as top-level producer output.
- [x] 4.2 Correct or supplement the existing Flutter preparation/upload test fixtures so the dedupe branch returns a URL correlated with the requested public ID and the new-upload branch consumes/returns a realistic canonical top-level `secure_url`. Retain coverage for nullable/unknown MIME metadata by using a canonical URL even when the response's optional `format` metadata is absent. Do not modify production Flutter code or add a cross-language fixture framework.
- [x] 4.3 Run `deno test --allow-env supabase/functions/prepare-cloudinary-upload/index_test.ts supabase/functions/_shared/cloudinary_test.ts` and `flutter test test/data/services/api/cloudinary/cloudinary_public_id_generator_test.dart test/data/services/api/cloudinary/supabase_cloudinary_upload_preparation_client_test.dart test/data/services/api/cloudinary/cloudinary_upload_client_impl_test.dart`. Require both producer branches and the lowercase SHA-256 public-ID generator to pass, then compare their accepted fixture shapes with the canonical URLs accepted by the submit parser in Task 2.2.

## 5. Prove validation-before-privilege and Subplan 5 compatibility

- [x] 5.1 Re-run `deno test supabase/functions/submit-content/index_test.ts` after implementation and require every representative invalid coordinate, structural-origin, duplicate, and invalid-replay request to produce zero store constructions and zero calls. Inspect `index.ts` to confirm complete validation still precedes `createStore()` and one `SubmissionStore.submit()` remains the only persistence path.
- [x] 5.2 Run `deno test supabase/functions/submit-content/submission_validation_test.ts supabase/functions/submit-content/index_test.ts supabase/functions/submit-content/submission_store_test.ts` and require all parser, handler, exact-forwarding, created/replayed/rate-limited, malformed-outcome, and persistence-failure tests to pass together.
- [x] 5.3 Inspect the production diff to confirm no validation moved into `submit_content`, `add_submission_assets`, Admin, Flutter, or importer code; no replay/hash comparison or idempotency-conflict outcome was added; and valid changed replays still use Subplan 5 first-commit-wins semantics.

## 6. Formatting, type checks, and change validation

- [x] 6.1 Run `deno fmt --check supabase/functions/_shared/cloudinary.ts supabase/functions/_shared/cloudinary_test.ts supabase/functions/prepare-cloudinary-upload/index_test.ts supabase/functions/submit-content/index.ts supabase/functions/submit-content/index_test.ts supabase/functions/submit-content/submission_validation.ts supabase/functions/submit-content/submission_validation_test.ts supabase/functions/submit-content/submission_store_test.ts` and require a zero exit with no unrelated formatting changes.
- [x] 6.2 Run `deno check supabase/functions/prepare-cloudinary-upload/index.ts supabase/functions/prepare-cloudinary-upload/index_test.ts supabase/functions/submit-content/index.ts supabase/functions/submit-content/index_test.ts supabase/functions/submit-content/submission_validation_test.ts supabase/functions/submit-content/submission_store_test.ts supabase/functions/_shared/cloudinary_test.ts` and require a zero exit.
- [x] 6.3 Run `dart format --output=none --set-exit-if-changed test/data/services/api/cloudinary/supabase_cloudinary_upload_preparation_client_test.dart test/data/services/api/cloudinary/cloudinary_upload_client_impl_test.dart` and re-run the focused Flutter command from Task 4.3; require no production Flutter diff and no test failure.
- [x] 6.4 Run `openspec validate harden-content-submission-edge-boundary-validation --strict --no-interactive` and require a zero exit with no warnings. Re-read proposal, design, spec, and tasks to confirm goals, requirements, implementation ownership, messages, non-goals, and verification commands remain mutually consistent.
- [ ] 6.5 Before deploying `submit-content`, verify through the target environment's approved secret/configuration mechanisms, without printing secret values, that: (a) `CLOUDINARY_CLOUD_NAME` is available to `submit-content` and identifies the same Cloudinary account used by `prepare-cloudinary-upload`; (b) the actual target Cloudinary upload preset referenced by `CLOUDINARY_CONTENT_UPLOAD_PRESET` is the intended preset for the public Content Submission flow, preserves the signed `content_submissions/<64-lowercase-hex-sha256>` public ID without an unexpected `public_id_prefix`, folder-derived or asset-folder-derived prefix (including `use_asset_folder_as_public_id_prefix` or equivalent behavior), otherwise leaves the final public ID within `content_submissions/<sha256>`, retains the expected `image/upload` resource/delivery identity, and produces top-level delivery URLs structurally compatible with `https://res.cloudinary.com/<configured-cloud-name>/image/upload/v<positive-decimal-version>/content_submissions/<lowercase-64-hex-sha256>.<lowercase-alphanumeric-format>`; and (c) the Subplan 5 `submit_content` RPC and supporting schema are already present in the target. Apply the preset gate only to the public Content Submission upload flow, leave trusted server-side importers and generic asset storage outside this policy, and block deployment on any missing, mismatched, or incompatible prerequisite.
- [x] 6.6 Run `git diff --check`, `git status --short`, and `git diff --name-only`; distinguish pre-existing user work from implementation-owned changes and require no migration, database/RPC, production Flutter, main spec, Subplan 1–5, archived change, unrelated OpenSpec, or `openspec/config.yaml` modification.

## 7. Final adversarial review gate

- [x] 7.1 Before considering Subplan 6 implementation complete, answer with evidence: (1) every URL shape both official public producer branches emit is accepted; (2) foreign accounts/namespaces/resource/delivery/non-canonical forms are rejected; (3) no global Cloudinary-only policy can break TicketOne or another trusted importer; (4) coordinates, structural origin, and duplicates all fail before store/RPC/replay/quota work; and (5) a committed idempotency identity cannot bypass current validation.
- [ ] 7.2 Continue the review with evidence that: (6) producer/consumer fixtures use the same public ID and delivery identity; (7) max-five, Delta, dates, asset metadata, quota, replay, and transaction semantics are preserved; (8) no speculative abstraction or scope expansion was introduced; (9) both the matching Cloudinary configuration gate and Subplan 5 database/RPC-before-Subplan 6 Edge deployment dependency are documented and verified; and (10) every previous OpenSpec artifact is untouched. Stop and remediate the plan or implementation if any answer is unsupported or any new material finding remains.
- [ ] 7.3 Report completion only as **Content Submission Hardening Subplan 6 complete: `submit-content` Edge boundary and validation hardening**. Do not claim whole-feature release readiness; keep the separate client error-taxonomy/`canRetry` mini-subplan and final Subplan 7 end-to-end hardening validation outstanding.
