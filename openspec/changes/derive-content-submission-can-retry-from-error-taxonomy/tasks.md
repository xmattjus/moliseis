## 1. Derived Immediate-Retry State

- [ ] 1.1 Add a read-only `ContentSubmissionViewModel.canRetrySubmissionImmediately` getter whose documented meaning is safe execution of the current immediate manual retry action; derive it only from `submit.running`, the current `submit.result`, and `submissionFinalizationPending`, returning true only for a stopped error with confirmed remote success pending local finalization. Add no backing field, setter, listener, domain failure interface, data-layer import, or HTTP/Supabase/Cloudinary inspection, and verify the focused ViewModel tests compile and pass.
- [ ] 1.2 Extend `test/ui/content_submission/view_models/content_submission_view_model_test.dart` to prove idle, running, success, and every ordinary error are false; failed finalization is true only after the Command stops; and starting another finalization attempt clears retry availability through existing Command state without synchronizing a second flag. Cover ordinary local eligibility, staged-state/checkpoint, upload, final-submit, malformed acknowledgement, transport, and unknown errors using existing fakes without adding production taxonomy types.
- [ ] 1.3 Strengthen the existing finalization regression so repeated failed local retirements and the eventual successful retry perform no preparation/checkpoint, asset upload, or `submit-content` call; preserve the acknowledged `clientSubmissionId`, current draft, and staged session after each failure; and rotate identity exactly once only after matching-session retirement succeeds. Run the focused ViewModel test file.

## 2. Verified Fail-Closed Boundary Coverage

- [ ] 2.1 Extend `test/data/repositories/content_submission_repository_impl_test.dart` only where coverage is missing to verify the current and rollout-relevant final-submit pairs preserve existing `ContentSubmissionApiException` fields: `VALIDATION_ERROR`/400, `UNAUTHORIZED`/401, `METHOD_NOT_ALLOWED`/405, `RATE_LIMIT_EXCEEDED`/429, current `INTERNAL_ERROR`/500, and legacy `RATE_LIMIT_READ_FAILED`, `RATE_LIMIT_UPDATE_FAILED`, `SUBMISSION_INSERT_FAILED`, `ASSET_INSERT_FAILED`, and `INTERNAL_ERROR`/500. Also verify missing/unknown/mismatched details, malformed positive acknowledgements, and final-submit `TimeoutException`, `ClientException`, `SocketException`, or equivalent injected transport failures remain errors after exactly one invocation. Do not add retry fields, wrappers, message parsing, or hidden requests; run the focused repository test file.
- [ ] 2.2 Extend `test/data/services/api/cloudinary/supabase_cloudinary_upload_preparation_client_test.dart` only where coverage is missing so validation/authentication/method/request-size/configuration failures, `CLOUDINARY_PREPARATION_ERROR`/502, unknown/mismatched Function responses, malformed success responses, and transport errors all remain ordinary errors. Assert no test treats 502, a message, reason phrase, or exception string as an immediate-retry discriminator, and run the focused preparation-client test file.
- [ ] 2.3 Re-run the existing `cloudinary_upload_client_impl_test.dart` regressions for internal HTTP-5xx/timeout attempts, exhaustion, content-addressed preparation/reuse, progress monotonicity, and cancellation. Add higher-layer coverage only for publicly observable terminal failures used by the fail-closed ViewModel test; do not expose `_UploadHttpException`, modify shared Cloudinary exceptions, wrap `ImageUploadTask`, add Admin behavior, or create tests for a direct-upload retry classification this plan intentionally does not implement.

## 3. Progress Actions and Route Recovery

- [ ] 3.1 Update only failed-state action selection in `ContentSubmissionProgressScreen` to consume `canRetrySubmissionImmediately`: a true value retains the existing `Riprova` execution of the submit Command, while every ordinary error keeps Home, omits `Riprova`, and exposes `Torna al modulo` that pops only the progress child route. Preserve visual hierarchy, styles, icons, spacing, unrelated wording, Back implementation, and all navigation ownership.
- [ ] 3.2 Update `content_submission_progress_screen_test.dart` so validation/authentication/method/rate-limit/internal-500, final-submit transport, malformed/unknown, preparation-502, direct-upload terminal, local, and unknown ordinary failures expose no `Riprova`; representative boundary tests MAY be table-driven or composed with lower-layer tests as long as no production UI-to-data dependency is introduced. Verify Home and `Torna al modulo` remain available and return-to-form performs no submit, clear, identity rotation, or staged cleanup.
- [ ] 3.3 Preserve and strengthen the finalization progress tests: a stopped finalization failure alone exposes `Riprova`; repeated activation remains local-only with no remote submit or upload; Back/Home remain blocked until success; identity and staged assets remain preserved across failure; and success behavior and one-time rotation remain unchanged. Run the focused progress-screen test file.
- [ ] 3.4 Run `test/routing/content_submission_route_test.dart` and `test/routing/content_submission_restoration_test.dart`, minimally extending them to verify ordinary-error Back and `Torna al modulo` pop exactly one progress route, preserve the existing form/session/staged state, push no duplicate form or progress route, and perform no remote or clear work. Require restored idle behavior, successful navigation, and running-state AppBar/system/predictive-Back blocking to remain unchanged.

## 4. Backend Evidence and Scope Verification

- [ ] 4.1 Run the unchanged `submit-content` handler/store/validation Deno tests and verify the hardened code still exposes only `VALIDATION_ERROR`/400, `UNAUTHORIZED`/401, `METHOD_NOT_ALLOWED`/405, `RATE_LIMIT_EXCEEDED`/429, and `INTERNAL_ERROR`/500 with created/replayed 201/200 acknowledgements and no 409. Inspect the legacy pre-`fe8a7e2` handler to confirm its separate quota/content/asset writes, legacy 500 codes, ignored `client_submission_id`, and absence of a server-generation/capability discriminator; require zero Edge, SQL, RPC, migration, RLS, idempotency, or deployment changes.
- [ ] 4.2 Run the unchanged `prepare-cloudinary-upload` Deno tests and require evidence that `CLOUDINARY_PREPARATION_ERROR`/502 covers both operational failures and deterministic invalid/configuration-mismatched prepared fields. Require zero preparation taxonomy or Edge implementation change.
- [ ] 4.3 Run `dart format --output=none --set-exit-if-changed` on every changed Dart file, targeted analysis, repository-wide `flutter analyze`, and together run `flutter test test/data/repositories/content_submission_repository_impl_test.dart test/data/services/api/cloudinary/supabase_cloudinary_upload_preparation_client_test.dart test/data/services/api/cloudinary/cloudinary_upload_client_impl_test.dart test/ui/content_submission/view_models/content_submission_view_model_test.dart test/ui/content_submission/widgets/content_submission_progress_screen_test.dart test/ui/content_submission/widgets/content_submission_screen_test.dart test/routing/content_submission_route_test.dart test/routing/content_submission_restoration_test.dart`; after the focused suite passes, run complete repository-wide `flutter test` and require zero failures from both focused and complete suites.
- [ ] 4.4 Audit the final implementation diff for no new exception or task wrapper, domain failure hierarchy, UI-to-data import, mutable retry flag, message/`toString()` parsing, package, generic `Command`/`Result`, shared Cloudinary/Admin, backend, automatic/delayed retry, unrelated ViewModel/UI, graphical, or release-operation change. Run `openspec validate derive-content-submission-can-retry-from-error-taxonomy --strict` and require a zero exit before marking implementation complete.
