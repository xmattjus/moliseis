## 1. Domain Semantic and Data-Boundary Classification

- [ ] 1.1 Add the minimal Content Submission-specific domain retry semantic under `lib/domain/` with only an immutable/read-only `canRetry` contract; verify domain code imports neither `data`, Supabase/HTTP, Cloudinary, UI, nor localized text and add/reuse a focused fake implementation in `test/support/` for higher-layer tests.
- [ ] 1.2 Make `ContentSubmissionApiException` implement the domain semantic using exact structured status/code pairs, preserving `statusCode`, `code`, and `message`; extend `test/data/repositories/content_submission_repository_impl_test.dart` to cover all verified pairs (`VALIDATION_ERROR`/400, `UNAUTHORIZED`/401, `METHOD_NOT_ALLOWED`/405, `RATE_LIMIT_EXCEEDED`/429, `INTERNAL_ERROR`/500), missing/unknown/mismatched pairs, string/reason/fallback messages, and zero message/`toString()` parsing, then run that test file.
- [ ] 1.3 At `ContentSubmissionRepositoryImpl.submit`, normalize only terminal `TimeoutException` into a Content Submission failure that retains the original cause and reports retryable; leave undifferentiated `http.ClientException`, `SocketException`, arbitrary, format, and client-contract exceptions fail-closed. Verify both accepted and rejected transport cases in `content_submission_repository_impl_test.dart` and assert each call still makes one request with no hidden retry.
- [ ] 1.4 Preserve structured `prepare-cloudinary-upload` `FunctionException` fields in a focused data-layer preparation failure without changing the Edge Function: exact `CLOUDINARY_PREPARATION_ERROR`/502 is retryable, while its verified validation/authentication/method/body-size/configuration pairs plus missing/unknown/mismatched outcomes fail closed. Extend `supabase_cloudinary_upload_preparation_client_test.dart` for the matrix, field preservation, transport propagation, and zero text parsing.
- [ ] 1.5 Preserve the existing `ImageUploadTask` API, progress stream, cancellation, Cloudinary request ordering, bounded internal retry, and content-addressed reuse while decorating terminal results at the existing Content Submission repository/upload boundary: terminal `TimeoutException` and exhausted direct-upload HTTP 5xx SHALL expose retryable semantics; generic client/socket, HTTP 4xx, validation, cancellation, format, filesystem, and unknown failures SHALL not. Verify with focused repository and `cloudinary_upload_client_impl_test.dart` cases that use structured exception/status types rather than text.
- [ ] 1.6 Because the Admin editor shares `ContentSubmissionRepository.uploadImageTask`, run its focused add-asset ViewModel/widget failure tests and verify the task decoration preserves generic Admin failure behavior, progress/cancel delegation, and the original diagnostic cause without adding Admin retry UX.

## 2. Derived ViewModel Retry State and Orchestration Invariants

- [ ] 2.1 Add a read-only `ContentSubmissionViewModel.canRetrySubmission` getter derived only from `submit.running`, `submit.result`, the error's domain retry semantic, and `submissionFinalizationPending`; add no backing boolean or setter, and extend `test/ui/content_submission/view_models/content_submission_view_model_test.dart` to prove idle/running/success are false, retryable ordinary error is true, permanent/unknown/local-preparation/checkpoint/staged-state/malformed errors are false, and starting a new execution cannot retain stale retryability.
- [ ] 2.2 Extend ViewModel orchestration tests so a legitimate retryable pre-acknowledgement failure followed by retry preserves the same `clientSubmissionId`, captured-session ownership rules, ordered staged paths, and Cloudinary reuse/dedup behavior; assert no premature clear, staged cleanup, or identity rotation and run the focused ViewModel test file.
- [ ] 2.3 Strengthen the existing finalization regression to assert a failed local retirement exposes retryability only after the Command stops running; repeated retries call neither attempt preparation/checkpoint, asset upload, nor remote submit; the acknowledged identity and staged session remain owned until one successful local-only finalization clears the matching session and rotates identity exactly once.
- [ ] 2.4 Preserve unexpected-error handling by testing thrown `Exception` and non-Exception `Object` paths: the Command reaches terminal error, pre-acknowledgement ownership releases, unknown retryability fails closed, and explicit edit/discard recovery remains available without changing generic `Command` or `Result`.

## 3. Progress Actions and Route Recovery

- [ ] 3.1 Update only the ordinary-error action selection in `ContentSubmissionProgressScreen`: when `canRetrySubmission` is true retain Home plus `Riprova` wired to the existing `submit.execute()` flow; when false retain Home, omit `Riprova`, and provide `Torna al modulo` that pops only the progress child route. Verify no visual hierarchy, style, icon, spacing, unrelated copy, clear/restore call, or new navigation mechanism is introduced.
- [ ] 3.2 Update `content_submission_progress_screen_test.dart` to use structured retryable test failures instead of generic `Exception` wherever `Riprova` is expected, and add permanent/unknown cases proving retry absence, safe return-to-form, preserved draft identity/data/staged assets, Home availability, and no submit/clear side effect; run the focused progress test file.
- [ ] 3.3 Re-run and minimally extend the existing progress/routing tests to prove finalization failure still has its local-only `Riprova`, success remains unchanged, running state still hides actions and blocks AppBar/system/predictive Back, ordinary failure Back still preserves the session, and no retry pushes a duplicate progress route.
- [ ] 3.4 Run `test/routing/content_submission_route_test.dart` and `test/routing/content_submission_restoration_test.dart`; require restored idle progress to retain `Torna al modulo` without submitting/clearing, parent-form exit policy to remain authoritative, and non-retryable recovery to pop exactly one progress route with no duplicate form.

## 4. Focused Verification and Scope Audit

- [ ] 4.1 Run `dart format --output=none --set-exit-if-changed` on every changed Dart test/production file and run targeted static analysis for the changed files; then run repository-wide `flutter analyze` and confirm no new in-scope diagnostic relative to the recorded checkout baseline.
- [ ] 4.2 Run together `flutter test test/data/repositories/content_submission_repository_impl_test.dart test/data/services/api/cloudinary/supabase_cloudinary_upload_preparation_client_test.dart test/data/services/api/cloudinary/cloudinary_upload_client_impl_test.dart test/ui/content_submission/view_models/content_submission_view_model_test.dart test/ui/content_submission/widgets/content_submission_progress_screen_test.dart test/ui/content_submission/widgets/content_submission_screen_test.dart test/routing/content_submission_route_test.dart test/routing/content_submission_restoration_test.dart` plus the focused Admin upload-failure tests identified in Task 1.6, and require all tests to pass.
- [ ] 4.3 Run `deno test supabase/functions/submit-content/index_test.ts supabase/functions/prepare-cloudinary-upload/index_test.ts`; require the unchanged handler tests to confirm exactly the five public `submit-content` failures with no 409 and the six structured preparation failures used by the client mapping, then inspect the production diff to require zero Edge Function, SQL/RPC/migration, server-idempotency, quota, or deployment change.
- [ ] 4.4 Audit the final production diff for no UI-to-data import, no message/`toString()` retry parsing, no mutable retry flag, no package or generic `Command`/`Result` change, no automatic/delayed retry, no Cloudinary architecture change, and no unrelated ViewModel/UI cleanup; run `openspec validate derive-content-submission-can-retry-from-error-taxonomy --strict` and require a zero exit before marking the implementation complete.
