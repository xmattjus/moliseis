## 1. Reconfirm the Post-Revert Baseline

- [x] 1.1 Before source edits, run `git status --short --branch` and `git log --oneline --decorate -10`; verify the implementation baseline still descends from the consecutive `abec1dc` documentation revert and `79e009c` staged-assets/editor-refresh implementation revert, record any user changes, and stop rather than borrowing code/design from `596ba9e` or `8065e1a` if the current draft files no longer match `design.md`.
- [x] 1.2 Reinspect `lib/domain/models/content_submission_draft.dart`, `lib/domain/repositories/content_submission_draft_repository.dart`, `lib/data/data-sources/content_submission_draft_entry.dart`, `lib/data/mappers/content_submission_draft_mapper.dart`, `lib/data/repositories/content_submission_draft_repository_impl.dart`, `lib/ui/content_submission/view_models/content_submission_view_model.dart`, `lib/config/dependencies.dart`, both generated ObjectBox files, and the tests named below; verify the fixed-ID single-draft and `Result` contracts are unchanged and stop on any material contradiction rather than widening scope.
- [x] 1.3 Run the current focused baseline `flutter test test/domain/models/content_submission_draft_test.dart test/data/mappers/content_submission_draft_mapper_test.dart test/data/repositories/content_submission_draft_repository_impl_test.dart test/ui/content_submission/view_models/content_submission_view_model_test.dart`; require it to pass before editing. Also capture the targeted analyzer output for the authored files in Task 5.1 so final verification can distinguish the four recorded pre-existing info diagnostics from newly introduced ones.

## 2. Persist the Stable Client Session Identity

- [x] 2.1 In `lib/domain/models/content_submission_draft.dart`, add non-null `clientSubmissionId`; generate omitted IDs as canonical UUID v4 from 16 `Random.secure()` bytes with correct version/variant bits and no insecure fallback, reject explicitly supplied malformed IDs, include identity in equality/hash, and preserve it through `copyWith` without exposing a normal form-edit replacement. Extend `test/domain/models/content_submission_draft_test.dart` with valid fixed UUID fixtures proving generation shape, identity-sensitive equality, field equality under one identity, and `copyWith` continuity; run that test and require it to pass.
- [x] 2.2 In `lib/data/data-sources/content_submission_draft_entry.dart`, add nullable stored `clientSubmissionId` while retaining assignable ObjectBox ID `1`. In `lib/data/mappers/content_submission_draft_mapper.dart`, round-trip valid identities and return no recoverable domain draft when the stored identity is absent or invalid before mapping legacy content; preserve every existing field/date/Delta mapping and the nullable repository surface. Extend `test/data/mappers/content_submission_draft_mapper_test.dart` for valid full/empty round trips and missing/invalid legacy identities, then run it and require it to pass without changing `ContentSubmissionDraftRepository` or introducing another model/repository.
- [x] 2.3 Regenerate ObjectBox output from the annotated entity rather than editing it: first use the installed build runner with filters for `lib/generated/objectbox.g.dart` and `lib/generated/objectbox-model.json`; if the ObjectBox builder cannot emit both through those filters, use `dart run build_runner build --delete-conflicting-outputs` and reject unrelated generated drift. Verify `ContentSubmissionDraftEntity` remains entity `20`, every existing property ID/UID and retired-ID record is preserved, exactly one new nullable string property ID/UID is added, and no Envied/dart_mappable or dependency file changes are retained.

## 3. Replace Autosave with Deterministic Checkpoints

- [x] 3.1 In `lib/ui/content_submission/view_models/content_submission_view_model.dart`, remove the debounce import, constructor callback, `_debounced` field, `_emit()` save call, and debounce disposal cleanup while leaving the shared `lib/utils/debounceable.dart` untouched; verify every form setter still updates/emits state but a fake repository observes zero saves immediately and after more than three seconds.
- [x] 3.2 Initialize one fresh draft and a private checkpoint baseline to the same snapshot, expose `hasUnsavedChanges` as `_state != _checkpointedDraft`, and keep all setters free of manual dirty assignments. Verify a fresh session is clean/write-free, each effective existing draft mutation is dirty, a semantically unchanged setter preserves the comparison result, and one `clientSubmissionId` survives all `copyWith` mutations.
- [x] 3.3 Add direct `Future<Result<void>> checkpointDraft()` on the ViewModel: capture `_state` before awaiting, return success without writing when it equals the baseline, await `saveDraft(snapshot)`, advance the baseline only to that captured snapshot on `Success`, notify active listeners, and return repository errors unchanged without advancing. Verify success, clean no-op, failure/retry, and A-saves-while-B-mutates behavior; do not add a `Command`, lock, queue, transaction coordinator, or production caller.
- [x] 3.4 Harden `initialize()` so a valid loaded draft and exact identity become both current state and clean checkpoint baseline with no save; keep the already-created fresh state/baseline on `Success(null)` or the existing load-error fallback. Verify valid recovery is clean and write-free and an unsupported legacy projection produces a fresh valid identity without save, clear, backfill, or migration.
- [x] 3.5 Reorder the existing `_clear()`/`clear` command to await `clearDraft()` before mutating form state, selected assets, transient event issue, baseline, or identity. On error, return the same `Result.error`, preserve the entire current session, and use the existing failure logging event; on success, clear in-memory submission state, generate a different identity, set the fresh snapshot as its clean baseline, notify, and log success. Verify pending, failed, and successful clear branches without adding navigation or UI error architecture.

## 4. Replace and Extend Regression Coverage

- [x] 4.1 Minimally extend `FakeContentSubmissionDraftRepository` in `test/support/fake_repositories.dart` using its neighboring completer/gate patterns so a test can hold one `saveDraft` or `clearDraft` call pending and complete it with a chosen `Result`; preserve existing counters/captured snapshots and verify existing fake consumers still compile without a parallel repository harness.
- [x] 4.2 In `test/ui/content_submission/view_models/content_submission_view_model_test.dart`, delete/replace the tests whose intent is to require three-second field, description, or terms autosave. Add focused regressions for fresh UUID/clean/no-write state; mutation plus beyond-three-seconds no-write behavior; derived dirty/no-op-setter behavior; explicit captured checkpoint; clean checkpoint no-op; failed checkpoint; same-ID retry/repeated checkpoints; controlled A/B in-flight mutation; exact-ID clean recovery/no initialization write; unsupported-legacy fallback; and successful/pending/failed discard state, asset, identity, dirty, result, and logging semantics. Run this file and require all cases to pass without timer flushing except the single negative former-interval case.
- [x] 4.3 Extend `test/data/repositories/content_submission_draft_repository_impl_test.dart` with the existing real `TestObjectBoxEnvironment`: prove exact identity plus all ordinary fields/rich Delta/date data survive save/load, a second save for the same session retains identity, clear removes the fixed record/content/identity and subsequent load is null, and a directly seeded pre-identity entity loads safely as no recoverable draft without an automatic write/delete before the next normal dirty checkpoint replaces it. Retain the existing rich Delta, 5,000-character, null-Delta, and incomplete-event tests and run the repository test to green.
- [x] 4.4 Update only affected fixtures/assertions in `test/ui/content_submission/widgets/content_submission_screen_test.dart`, `test/routing/content_submission_restoration_test.dart`, and other current `ContentSubmissionDraft` consumers needed for the new identity invariant. Remove debounce drains whose only purpose was implicit saving; preserve the screen's loading presentation while clear is pending, but assert the ViewModel retains the old session until success. Run the screen and restoration tests and require no navigation, progress-route, submit-result, asset, or remote-submit behavior changes.

## 5. Verify the Focused Change

- [x] 5.1 Run `dart format --output=none --set-exit-if-changed` over authored/touched Dart source and tests, excluding generator-owned Dart, and run targeted `flutter analyze` for `lib/domain/models/content_submission_draft.dart`, `lib/data/data-sources/content_submission_draft_entry.dart`, `lib/data/mappers/content_submission_draft_mapper.dart`, `lib/data/repositories/content_submission_draft_repository_impl.dart` if changed, `lib/ui/content_submission/view_models/content_submission_view_model.dart`, `test/domain/models/content_submission_draft_test.dart`, `test/data/mappers/content_submission_draft_mapper_test.dart`, `test/data/repositories/content_submission_draft_repository_impl_test.dart`, `test/ui/content_submission/view_models/content_submission_view_model_test.dart`, `test/support/fake_repositories.dart`, and any touched screen/restoration test. Require no new or in-scope diagnostics relative to Task 1.3; do not require zero exit while the recorded positional-boolean infos remain or clean unrelated lints.
- [x] 5.2 Run `flutter test test/domain/models/content_submission_draft_test.dart test/data/mappers/content_submission_draft_mapper_test.dart test/data/repositories/content_submission_draft_repository_impl_test.dart test/ui/content_submission/view_models/content_submission_view_model_test.dart test/ui/content_submission/widgets/content_submission_screen_test.dart test/routing/content_submission_restoration_test.dart` and require all focused persistence/recovery plus surrounding screen/restoration regressions to pass. Then run `flutter test` and require the broader suite to pass because the domain constructor/equality and shared fake are cross-test contracts.
- [x] 5.3 Run repository-wide `flutter analyze` and compare it with the recorded 189-diagnostic baseline: require no new or in-scope diagnostics while treating unrelated pre-existing diagnostics as baseline, not cleanup work. Verify generated correctness through the successful ObjectBox generation, stable model IDs/UIDs, and focused real-store tests rather than manually linting or changing `lib/generated/objectbox.g.dart`.
- [x] 5.4 Inspect `git status --short`, `git diff --stat`, `git diff`, and `git diff --check`; require the final implementation diff to stay within the domain draft, ObjectBox entity/mapper (and repository implementation only if compilation requires its nullable mapper result), ViewModel, generated ObjectBox pair, existing test support/tests, and these OpenSpec artifacts. Reject dependency changes, `debounceable.dart` edits, remote DTO/repository/payload changes, Supabase/Edge Function/RPC/RLS/migration files, Admin, navigation, asset/Cloudinary, multi-draft/session-framework, or reverted-commit drift.
- [x] 5.5 Re-read `proposal.md`, the capability delta, `design.md`, and this checklist against the final implementation, then run `openspec validate harden-content-submission-draft-persistence --strict --no-interactive` and require a zero exit. Handoff is complete only when form mutation never writes implicitly; checkpoint success/failure and in-flight mutation preserve exact snapshot semantics; identity survives persistence/recovery/retry; successful discard clears before rotation; failed save/clear never creates false-clean or rotated state; and every scoped test, baseline-aware analysis, diff, and OpenSpec gate above is satisfied.
