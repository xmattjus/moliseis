## 1. Revalidate the Implemented Baseline and Footprint

- [x] 1.1 Before source edits, inspect `git status --short --branch`, recent history, `AGENTS.md`, and all artifacts for `harden-content-submission-draft-persistence` and `harden-content-submission-local-asset-staging`; verify HEAD still contains the completed checkpoint, identity, staged-asset, lifecycle-queue, retry, and persistence-first-clear contracts described in `design.md`, preserve any user changes, and stop for a planning update rather than redesigning if a material contract has changed.
- [x] 1.2 Reinspect `lib/ui/content_submission/view_models/content_submission_view_model.dart`, both Content Submission screens, `lib/routing/router.dart`, `lib/utils/command.dart`, Provider composition, URL-launch services, focused tests, and installed `go_router` 17.3.0 `GoRoute.onExit`/delegate source. Confirm specifically that each pop callback resolving true independently calls `_completeRouteMatch`, configuration replacements consume their own affirmative result, and `pushNamed` synchronously records/notifies its push before returning the route-completion Future. Run the current focused baseline command `flutter test test/ui/content_submission/view_models/content_submission_view_model_test.dart test/ui/content_submission/widgets/content_submission_screen_test.dart test/ui/content_submission/widgets/content_submission_progress_screen_test.dart test/routing/content_submission_restoration_test.dart`; require it to pass and stop if parent-route removal, child-pop, push handoff, or Command behavior contradicts this plan. Also retain the complete pre-edit `flutter analyze` output so post-edit comparison can match severity/code/path/location/message tuples rather than relying only on the recorded total of 189 diagnostics.
- [x] 1.3 Confirm current Supabase migrations, `submit-content` Edge Function, RPC/RLS tests, ObjectBox model, remote repository contracts, and dependency files require no change. Record the pre-edit production footprint as the four files listed in `design.md`; reject backend, generated-code, package, repository-interface, `Command`, Admin, or new architectural-layer edits unless a material contradiction is first reconciled through OpenSpec.

## 2. Add the Minimum ViewModel Lifecycle Semantics

- [x] 2.1 In `ContentSubmissionViewModel`, add one direct `Future<Result<void>>` operation that awaits initialization, enters the existing `_serialize` boundary, and restores dirty `_state` to `_checkpointedDraft` while clearing only transient event-time validation state and notifying listeners. Return a no-op success when already equal; return the retained load error without mutation when persisted ownership is `unknown`; never call draft save/clear, staged cleanup, rotate identity, or mutate assets. Extend `content_submission_view_model_test.dart` to prove successful restore, clean no-op, unknown-state failure, unchanged checkpoint/identity/assets, zero draft-clear/save calls, and later reopen-visible state.
- [x] 2.2 Add only the transient confirmed-remote-success/local-finalization-pending flag and read-only getter from `design.md`. Refactor `_submit()` so ordinary remote errors preserve the current session, remote success marks finalization pending, explicitly awaits `clear.execute()`, and inspects `clear.result` only after that Future completes; clear success returns submit success after one fresh rotation, and clear error retains the pending marker and returns an error without abandoning work. Verify submit remains running until clear terminates, submit remains `Command0<void>`, `clear` remains the existing Command, and `lib/utils/command.dart` plus repository interfaces remain unchanged.
- [x] 2.3 At `_submit()` entry, make a pending-finalization retry bypass form validation, staged/Cloudinary uploads, and final remote upload and retry only the same clear-based local finalization. Add controlled ViewModel regressions proving: remote success waits in running state for a pending clear; success clears the persisted draft and old staged session before submit completes; initial clear failure preserves identity/state/assets; later Retry produces zero additional remote/image upload calls; repeated local failures remain retryable; and the one eventual successful rotation/fresh identity occurs exactly once.

## 3. Centralize Form Removal in the Existing Route

- [x] 3.1 In `lib/routing/router.dart`, create one route-local nullable `Object` ownership token with two closures: acquire stores/returns a fresh token only when none is active, and release clears only an identical token. Add `GoRoute.onExit` only to the existing Content Submission parent route; every invocation must acquire before checking clean/dirty state and return its own `false` immediately if acquisition fails. The owner implements one private three-value dialog choice enum, exact labels `Salva ed esci`, `Esci senza salvare`, and `Annulla`, null/dismissal-as-cancel behavior, checkpoint/restore Result handling, and mounted-safe generic error feedback. Never memoize or return the owner's Future to an overlapping invocation.
- [x] 3.2 Release the owning route token immediately after cancellation, operation failure, or a caught unexpected exception. On either clean or dirty affirmative completion, retain that exact token until an identity-checked post-route-removal frame callback releases it, so a stale overlapping invocation cannot observe the newly clean ViewModel and produce a second true before go_router consumes the first. Do not call `clear`, add a coordinator/service/Command/general lock, or add a form `PopScope`; only if Task 1.2 disproves `onExit` coverage may a form `PopScope` be added as a transient `boundaryPending` back lock with no dialog, dirty/clean decision, persistence, restoration, clear, or navigation behavior.
- [ ] 3.3 Add `test/routing/content_submission_route_test.dart` using `buildAppRouter` rather than copying the `onExit` callback into a test router. Follow the existing production-router harness pattern: construct the router with a non-due `SyncViewModel` and `ControllableAdminAuth`, provide exactly one application-scoped Content Submission ViewModel and the form's URL service, navigate to the submission location before first pump to avoid unrelated home-shell dependencies, and dispose all owned objects. Prove clean exit has no dialog/write; dirty AppBar and OS-back attempts show exactly the three choices; `Annulla` changes neither route nor state; `Salva ed esci` exits only after successful checkpoint; checkpoint failure blocks exit, shows generic feedback, and allows a later exit attempt. Verify the defensive exception denial and identity-checked release structurally during Task 6.6 rather than adding a production injection seam solely to force an otherwise unreachable widget-test exception.
- [ ] 3.4 With a controllable checkpoint/restore gate and a sentinel route beneath the form, start two overlapping Back/pop attempts and prove exactly one dialog, one selected checkpoint or restore, one affirmative owner, one form removal, and preservation of the sentinel route. Assert no double/stale route-match completion, Flutter exception, or go_router exception; repeat for cancel and persistence failure, then prove a later independent attempt works normally after cancel/failure and after reopening following successful completion.
- [ ] 3.5 Extend the same route coverage for `Esci senza salvare`: seed a real checkpoint/persisted draft, make later form edits, exit through checkpoint restore, reopen the application-scoped ViewModel route, and prove discarded values do not reappear while persisted draft content, client identity, and durable staged assets remain and draft/session clear is never called. Also cover restore failure from unknown persisted ownership as a blocked exit with generic feedback.
- [ ] 3.6 Use `test/support/predictive_back.dart` to prove Android predictive-back commit enters the same clean/dirty route policy and does not remove a dirty form when cancelled, denied, or overlapped. Exercise concurrent programmatic removals with distinguishable destinations and a gated owner: prove only the owner's destination applies, the overlapping replacement receives no shared affirmative result, and later programmatic removal works after ownership release. Also prove popping only the nested progress route is not treated as a parent-form exit. Keep platform policy shared and leave the native iOS interactive gesture to the explicit manual check in Task 6.5.
- [ ] 3.7 Add the Flutter 3.41.9-scoped dirty-iOS gesture disposition to `ContentSubmissionScreen`: prevent only dirty native interactive pop initiation through the minimum `PopScope` mechanism, keep Android system/predictive behavior and clean iOS swipe unchanged, and provide a transient AppBar-Back allowance that reaches the existing `GoRoute.onExit` policy without duplicating it. Derive the accessible `● Modifiche non salvate` subtitle from `hasUnsavedChanges` inside unchanged standard `SliverAppBar` geometry. Add regressions for clean/dirty pop disposition, unchanged Back control, dirty indicator visibility, checkpoint disappearance, explicit-Back `Annulla`, and invariant AppBar extent.

## 4. Harden Form-Owned External and Submission Boundaries

- [ ] 4.1 Before adding test doubles, inventory `test/support/` and all existing URL-launch fakes. Add one focused shared recording `ExternalUrlService` fake under `test/support/` only if no current symbol can express Terms/Privacy call ordering and failure; compose the real `UrlLaunchService` around it, preserve fail-fast/default behavior, and run every existing/new consumer test. Do not copy the private fake from `url_launch_service_test.dart` or add a production abstraction.
- [ ] 4.2 Add required route-token acquire/release function parameters to `ContentSubmissionScreen` and supply them from the existing production route; update its sole direct widget-test construction with a narrow deterministic gate, without adding a production interface/type. Replace the duplicate inline Terms/Privacy callbacks with one mounted-safe helper that acquires synchronously and returns without checkpoint/launch when ownership is unavailable. After acquisition, set/release the existing planned local flag with mounted-safe `setState`, and wrap the existing `CustomScrollView` once in `AbsorbPointer(absorbing: boundaryPending)` so the complete form/asset/link/submit/AppBar pointer subtree is gated without per-control branches.
- [ ] 4.3 For Terms and Privacy, capture dependencies before await, checkpoint only dirty state, block launch and show the existing generic snackbar on checkpoint error, retain existing snackbar behavior on launcher failure, and release the route token in `finally` even after unmount while touching local State only when mounted. Add widget tests proving each destination launches only after its checkpoint, failure prevents each launch and preserves work, clean launch avoids an unnecessary empty-draft save, rapid duplicate activation remains single-flight, denied route-token acquisition starts no checkpoint/launch, the token releases after success/failure/unmount, pointer attempts cannot mutate a representative field or invoke the asset picker while gated, and unmount during a gated checkpoint produces no framework exception or late launch.
- [ ] 4.4 Preserve ImagePicker ownership unchanged: add/update a screen-level regression that drives the actual add-asset action and proves only the existing ViewModel pre-picker checkpoint occurs before one picker invocation. Verify no UI checkpoint or route-token acquisition is added around `addAsset`, no asset/staging behavior is duplicated, and predecessor picker/recovery tests remain green.
- [ ] 4.5 Refactor the `Invia` callback into a mounted-safe State method using the same local flag and route token. Preserve both form validations and event-time validation before lifecycle work; after validation, acquire synchronously and return without checkpoint/submit/push when unavailable. Once acquired, absorb edits, await `checkpointDraft()`, show generic feedback and start neither submit nor navigation on error, then start `submit.execute()` before one `pushNamed` call. Release the route token immediately after the synchronous push request is accepted so progress Home can later reach parent `onExit`; do not await the route-completion Future because `goNamed` replacement need not complete it. Keep the local flag through the push frame and release it in a mounted post-frame callback, while identity-safe token release and mounted guards ensure every failure/unmount path frees ownership without late `State.widget`/`State.context` access.
- [ ] 4.6 Extend `content_submission_screen_test.dart` to prove invalid forms/events perform no token acquisition/checkpoint/submit/push; successful checkpoint precedes exactly one submit and progress push; checkpoint failure preserves editable work and releases ownership; denied acquisition starts nothing; the route token releases after synchronous push handoff while the local interaction flag remains set through that frame and then releases; a gated checkpoint blocks pointer mutation; rapid repeated taps cannot create multiple checkpoints, remote calls, or progress routes; and unmount before checkpoint completion causes no late submit/navigation or exception.
- [ ] 4.7 In the production-router suite, use controllable checkpoint gates rather than delays to hold pre-submit, Terms, and Privacy boundaries after token acquisition. During each pending checkpoint, commit system Back and Android predictive Back and prove the removal is denied with no exit dialog, no second checkpoint/restore, and no route removal; after releasing the gate, prove at most one expected submit/progress or external launch occurs and normal Back policy resumes after the boundary completes or fails.
- [ ] 4.8 Exercise the inverse ownership order deterministically: hold an owning dirty-exit checkpoint/restore decision, then invoke the otherwise reachable submit and Terms/Privacy callbacks through the test widget callbacks while the modal barrier is present. Prove denied form acquisition creates no boundary checkpoint, submit execution, progress push, or external launch; let the exit win and assert no late continuation or exception after form removal. After cancellation/failure instead, prove a later independent form action and Back attempt each acquire and behave normally.

## 5. Make Progress Presentation Navigation-Only

- [x] 5.1 In `ContentSubmissionProgressScreen`, remove `_clearFuture`, `_clearOnce`, post-pop clear scheduling, and `_clearState`. Retain the existing `PopScope` and dedicated route, but derive pop/action availability from `submit.running` and finalization pending: running blocks all exits; idle permits Back/`Torna al modulo`; ordinary remote error permits Back/`Riprova`/Home; finalization error blocks all navigation and offers only finalization-only `Riprova` with explicit non-resend copy; completed permits Back/`Nuovo suggerimento`/Home. All remaining buttons must only pop, go Home, or execute the existing submit Command.
- [ ] 5.2 Refactor `content_submission_progress_screen_test.dart` away from progress-owned cleanup assertions. Preserve and extend observable coverage for AppBar/system/predictive back while running; restored idle return; remote failure Back preserving form/identity/assets; remote failure Retry using the same session; remote failure Home performing no clear and delegating any dirty parent exit to the route guard; and no duplicate submit while running. In the production-router test, use Home's `goNamed` replacement after a real form push and prove it reaches Home without waiting for the original `pushNamed` completion, leaves no active route token/stale form callback, and emits no framework/router exception.
- [ ] 5.3 Add progress regressions for confirmed-remote-success/local-finalization failure: distinct text/state, no Back/AppBar/Home/new-submission action, blocked OS and Android predictive back, one visible `Riprova`, no second remote or image upload after retry, and transition to completed only after clear succeeds. Verify repeated clear failures remain on progress without memoizing an unrepeatable failed Future.
- [ ] 5.4 Add success regressions proving local finalization occurs before success UI, progress navigation performs no extra clear, Back and `Nuovo suggerimento` each reveal the already-clean new form, Home leaves no stale completed draft/session, staged assets for the completed identity are no longer active, and multiple progress navigation interactions cannot rotate or clear the new session again.
- [ ] 5.5 Update `content_submission_restoration_test.dart` only as required to preserve its production-shaped contract: process-restored progress has an idle Command, Back/`Torna al modulo` returns to the form without retry or destructive clear, and the pre-submit checkpointed draft remains recoverable. Do not invent persisted Command/finalization state or claim the unresolved backend-acknowledgement crash window is solved.

## 6. Verification, Minimal-Diff Review, and Handoff

- [ ] 6.1 Run `flutter test test/ui/content_submission/view_models/content_submission_view_model_test.dart test/ui/content_submission/widgets/content_submission_screen_test.dart test/ui/content_submission/widgets/content_submission_progress_screen_test.dart test/routing/content_submission_restoration_test.dart test/routing/content_submission_route_test.dart test/data/services/url_launch_service_test.dart`, plus every other consumer of changed shared test support; require all checkpoint, one-owner/one-affirmative exit, sentinel-route preservation, form-boundary mutual exclusion, no-late-continuation, finalization, navigation, system-back, and predictive-back scenarios to pass, then run `flutter test` and require the full suite to pass.
- [ ] 6.2 Run `dart format --output=none --set-exit-if-changed` over authored/touched Dart source and tests, excluding generated Dart, then targeted `flutter analyze` over every touched source/test file. Require zero new or in-scope diagnostics relative to the recorded existing infos/warnings in `ContentSubmissionViewModel`, the form, progress screen, and any reused support file; do not broaden this change into unrelated lint cleanup.
- [ ] 6.3 Run repository-wide `flutter analyze` and compare every diagnostic tuple (severity, code, path, line/column, and message) with the retained pre-edit output; require no new or worsened in-scope diagnostics and use the measured HEAD total of 189 only as a supplementary cross-check, not as permission to replace one failure with another. Run `git diff --check`, inspect `git status --short`, `git diff --stat`, and the complete diff, and reject generated, dependency, backend, Admin, generic `Command`, unrelated UI, or speculative architecture drift.
- [ ] 6.4 Perform the mandatory second minimal-diff accounting with `git diff --numstat -- lib/routing/router.dart lib/ui/content_submission/view_models/content_submission_view_model.dart lib/ui/content_submission/widgets/content_submission_screen.dart lib/ui/content_submission/widgets/content_submission_progress_screen.dart`: production files should remain those four, with only the existing route and form additionally participating in concurrency coordination through two function callbacks and one nullable identity token. Add no production file, extra type beyond the dialog enum, layer, package, persistent field, generalized lock, or form dirty-exit policy. Calculated production additions minus deletions excluding tests/docs/generated output should remain in the `100–250 LOC` net category; record gross additions/deletions and net result. If net growth approaches or exceeds 250 lines, stop and simplify duplicated route/form/progress/ViewModel logic; if correctness genuinely still requires `>250 LOC`, update `design.md` with block-by-block justification before handoff.
- [ ] 6.5 **Manual iOS gate — remains open after observed failure on iOS Simulator 26.5.** From a fresh/reliable navigation state, rerun the revised full matrix: clean complete native swipe; dirty indicator/unchanged AppBar geometry; dirty edge swipe does not begin or expose Explore; dirty AppBar Back → `Annulla`; dirty AppBar Back → `Salva ed esci`; dirty AppBar Back → `Esci senza salvare`; clean partial swipe cancellation; and reachable clean-state transition sanity. Record device/runtime and each result. Do not complete this task until the user explicitly reports every revised case passing.
- [ ] 6.6 Re-read `proposal.md`, the capability spec, `design.md`, and this checklist against the final implementation; run `openspec validate harden-content-submission-exit-progress-route --strict --no-interactive`; and require an independent semantic review of single-consumer affirmative exit ownership, identity-safe delayed release, both form-boundary/exit race orders, push-handoff release, async mounted safety, persistence ownership, remote-success/finalization-failure no-resend behavior, failure preservation, and the minimal-diff budget. Handoff is complete only when all objective gates pass and the result is reported as Subplan 3, not whole Content Submission release readiness.
