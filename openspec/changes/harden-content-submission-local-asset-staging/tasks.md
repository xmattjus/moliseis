## 1. Local Staged Model and Persistence

- [x] 1.1 Reconfirm the Subplan 1 and asset baseline before editing: inspect `ContentSubmissionDraft`, `ContentSubmissionViewModel`, `ContentSubmissionDraftRepository`/implementation, `ContentSubmissionScreen`, `ContentSubmissionAssetList`, `Command`, `dependencies.dart`, ObjectBox generated metadata/query conventions, `SubmissionAsset`, the current `_submit()` upload loop, and the completed `harden-content-submission-draft-persistence` artifacts; run the current focused Content Submission tests and stop on any material contradiction with `design.md` rather than importing the reverted staged-asset implementation from `596ba9e`.
- [x] 1.2 Add immutable `lib/domain/models/content_submission_staged_asset.dart` containing only `clientSubmissionId`, `digest`, and `relativePath`, plus a separate auto-ID `ContentSubmissionStagedAssetEntity` under `lib/data/data-sources/` and a mapper only if needed by current data-boundary conventions; verify model/mapper tests reject or avoid malformed ownership/path values and confirm no picker, Cloudinary, upload-status, timestamp, source-path, MIME, dimension, thumbnail, or byte fields were added.
- [x] 1.3 Add `lib/domain/repositories/content_submission_staged_asset_repository.dart` with the focused reconcile/load, stage-acquire, absolute-path resolution, single removal, and idempotent per-session cleanup responsibilities from `design.md`, then add the implementation foundation under `lib/data/repositories/` using `Result<T>`, the shared `Logger`, the app `ObjectBox` wrapper, and explicit query closure; verify the domain layer does not import data/UI types and no repository/manager/filesystem-service stack was introduced.
- [x] 1.4 Give the implementation a synchronous constructor accepting `Logger`, `ObjectBox`, and an optional `Future<Directory> Function()` defaulting to `getApplicationSupportDirectory`, memoize its first directory future, register `ContentSubmissionStagedAssetRepository` before `ContentSubmissionViewModel` in `lib/config/dependencies.dart`, and inject the interface into the ViewModel. Update existing ViewModel construction sites/shared fakes minimally, and verify production uses the default while real tests return their temporary support directory without service-locator or direct ObjectBox access from the ViewModel.
- [x] 1.5 Run the narrowest verified ObjectBox build_runner command that emits both `lib/generated/objectbox.g.dart` and `lib/generated/objectbox-model.json`; if filters cannot emit both, run `dart run build_runner build --delete-conflicting-outputs`. Verify the generator retained every existing entity/property ID and UID, added only the new staged entity/properties, used an auto-increment staged ID, added no `position` property, and produced no unrelated Envied/dart_mappable or dependency drift.

## 2. Filesystem Staging and Reconciliation

- [x] 2.1 Implement the private staging root `<application-support>/content_submission/staged`, per-session UUID-v4 directory, deterministic lowercase SHA-1 final basename, exact temporary basename `.<digest>.<random-hex>.tmp`, and relative path `<clientSubmissionId>/<digest>`; enumerate immediate children with links disabled and reject/unlink symlink entries instead of traversing them. Add focused tests proving paths resolve inside the root, malformed digest/session/relative paths cannot escape, symlinked state is never followed, and picker source paths are never persisted.
- [x] 2.2 Implement idempotent stage/acquire for an initially size-validated source and caller-supplied digest: query by active identity+digest, copy to a temporary file, await/close the write, recheck the temp against `kCloudinaryMaxUploadBytes`, hash it and require the supplied SHA-1, rename to the deterministic final file, persist the descriptor only afterward, and return the committed descriptor. In real temporary filesystem/ObjectBox tests, prove staged bytes are independent from the source, a stale supplied digest cannot mislabel copied bytes, oversized input cannot commit, no descriptor references a temp file, repeated same-session staging creates one file/descriptor, and identical content under another identity creates separate ownership.
- [x] 2.3 Cover practical stage failure behavior in repository tests: a missing/copy-failing source returns `Result.error` without a committed descriptor and a later failed candidate does not remove earlier committed assets. Document the accepted descriptor-persistence crash window as orphan cleanup rather than retaining operation hooks for exact rename/write interruption injection; avoid an all-or-nothing transaction, journal, or state machine.
- [x] 2.4 Implement active-session reconciliation using explicit ascending `ContentSubmissionStagedAssetEntity_.id` ordering: validate each descriptor-backed final once, delete stale/malformed rows by technical ID without dereferencing their path, remove exact-pattern temp files and descriptor-less finals as orphan state, retain the lowest valid auto-ID duplicate and delete later duplicates, and return valid descriptors in explicit ID order without reconstruction queries. Verify content/name/size mismatch and assert stale rows/files are removed while unrelated valid active assets retain explicit ID order.
- [x] 2.5 Implement non-active orphan reconciliation by enumerating descriptor identities and immediate staging-root children without following links: clear every valid identity unequal to the active persisted ID, remove malformed in-root entries safely, unlink symlinks themselves, and treat all staged state as orphaned only when authoritative persisted-draft absence is established. Verify foreign/malformed/linked descriptors/files/directories are never returned or followed outside the root, temporary files and empty directories are removed, repeated cleanup is safe, and an old directory is never adopted by a newly generated identity.
- [x] 2.6 Implement single-asset removal and `clearSession(clientSubmissionId)` in the repository: delete matching descriptors, deterministic final files, temporary files, and the empty per-session directory idempotently. Verify removal deletes descriptor+file, succeeds with an already-missing file, full cleanup covers mixed state, and repeated full cleanup remains safe.

## 3. Durable Checkpoint Integration and Lifecycle Arbitration

- [x] 3.1 In `ContentSubmissionViewModel`, use persisted-draft `unknown`/`absent`/`restored` state as the sole durable-existence authority beside `_checkpointedDraft`: only `restored` may skip an equal snapshot, successful save/load establishes `restored`, and successful rotation establishes `absent`. Extend ViewModel tests to prove the initial empty equal draft is physically saved, checkpoint failure remains non-durable, success makes a later unchanged checkpoint a no-op, valid restore is immediately durable/write-free, and a post-clear fresh identity again requires its first save.
- [x] 3.2 Make `initialize()` memoize one shared future, keep DI's unawaited startup call, and make public checkpoint plus add/remove/clear await that same future before lifecycle arbitration (calling them first starts initialization); recovery must still acquire `retrieveLostData()` before awaiting, including when that platform call throws. Add controlled tests proving load/reconcile run once, early add cannot checkpoint the constructor draft over a pending persisted load, early checkpoint/clear cannot be overwritten by late initialization, and a direct thrown recovery attempt still completes initialization.
- [x] 3.3 Add one private resilient async serialization helper for Content Submission local asset/session mutation phases and standalone checkpoint writes, ensuring its tail always releases after `Result.error` or a thrown exception; implement a private already-inside-boundary checkpoint body so public `checkpointDraft()` and addAsset do not nest the lock. Do not modify `lib/utils/command.dart` or add a package/global scheduler. Add controlled tests proving different Commands serialize, failure does not poison later work, and checkpoint/clear ordering cannot save a retired identity after clear.
- [x] 3.4 Split `_addAsset()` into a post-initialization serialized pre-picker phase and post-picker commit phase: before the picker, retry any unavailable staged reconciliation, await the durable checkpoint, capture the active `clientSubmissionId`, and leave arbitration; only then invoke existing `pickMultipleMedia(limit: ...)`. Verify exact ordering/identity, no picker call on initialization/reconciliation/checkpoint error, no asset mutation on failure, and unchanged no-picker behavior at capacity.
- [x] 3.5 Re-enter arbitration after picker completion, compare the captured identity before candidate inspection, and treat a mismatch as a successful stale/cancelled no-op with no generic UI error or path-bearing log. Treat disposal as terminal only at operation entry and runtime publication/notification boundaries while preserving already-started durable work. Reproduce draft A picker pending → successful clear/rotation to B → picker A returns, and assert no candidate hash/copy, temporary/final file, descriptor, or runtime asset is created in either session; verify disposal prevents late runtime mutation or framework notification without testing every intermediate await.
- [x] 3.6 Gate the staged repository's commit phase and `clear()` with controlled Completers to prove the opposite ordering: when staging entered arbitration first, clear waits, then removes the committed old-session staged state before rotating. Also overlap `removeAssetAt` with stage and clear to prove it observes a complete serial predecessor and cannot remove or retain an asset under the wrong identity.

## 4. Runtime Staged-Asset Integration

- [x] 4.1 Replace separate hash/add logic with one internal candidate routine used by normal selection and later Android recovery: calculate current capacity and existing 10 MiB validation, stream the existing SHA-1 digest, soft-skip active-session duplicates, call staged acquire, then create the unchanged runtime `Asset` shape as `XFile(stagedAbsolutePath)` plus digest only after commit. Verify no SHA-256 reuse, new package, or public Command/`AssetSelectionOutcome` rename.
- [x] 4.2 Update normal picker processing to use the common routine while preserving current picker limit hints, maximum-five behavior, soft oversized/over-capacity outcomes, batch continuation, and current duplicate/no-backfill semantics. Rewrite existing add-asset assertions to use real temporary source files and a staged fake/repository, proving newly added `Asset.file.path` is inside Content Submission staging and differs from the picker source.
- [x] 4.3 Coordinate `initialize()` as one memoized future performing draft load → durable-active-ID decision → staged reconcile/load (or all-orphan cleanup only after `Success(null)`) → ordered runtime `Asset` reconstruction → `loadState = ready`/notification. A draft-load error must leave persisted ownership unknown, skip destructive reconciliation, preserve staged descriptors/files without adoption, and block checkpoint/picker writes of the fresh identity. Gate reconciliation in tests to prove `loadState` remains loading and no partial list is exposed, then prove ViewModel recreation restores the same valid staged paths/digests in explicit auto-ID order and maximum capacity includes restored assets. On hard reconciliation error, store it, expose no partial assets, complete the shared future/ready transition, and require addAsset's pre-picker phase to retry reconciliation and return the error without picker invocation while state remains unavailable. `_submit()` must await initialization and reject unavailable/quarantined staged state before any Cloudinary or final remote operation, leaving runtime assets unexposed and durable descriptors/files unchanged; cover reconciliation error, path-resolution error, and over-capacity quarantine.
- [x] 4.4 Preserve the existing `ContentSubmissionAssetList` contract and minimal UI: continue rendering `Image.file(File(asset.file.path))`, using existing add/remove/recovery Commands and warning behavior. Update only affected widget fixtures/tests to use valid staged paths, and verify thumbnails render from staged files, add hides at five restored assets, and no UI/navigation redesign was introduced.

## 5. Android Lost-Data Recovery

- [x] 5.1 Use the memoized initialization future directly as the completion point after staged reconciliation; expose no second barrier or new screen-timing dependency. Verify ordinary initialization, no-draft fallback, draft-load error, staged-reconciliation error, and a thrown `retrieveLostData()` call without prior initialization do not leave recovery waiting forever, while untrusted/partially restored staged state cannot be mutated.
- [x] 5.2 Keep Android `ImagePicker.retrieveLostData()` acquisition before awaiting the shared initialization future, retain its response in that async Command invocation, then capture the recovery-eligible restored identity and enter local lifecycle arbitration; compare that identity with the current active ID before processing so clear can make the response stale. Add the production-order regression: construct ViewModel, start `initialize()` without awaiting, trigger recovery as `ContentSubmissionScreen.initState()` does, return lost media first, complete restored draft+staged reconciliation later, and assert staging begins only afterward under the restored identity.
- [x] 5.3 Prioritize non-null `response.files` without preserving the current `response.file != null` outer gate, fall back to `response.file` only for a single result, and route recovered candidates through the common pipeline. Test restored-state deduplication, oversized rejection, remaining capacity after four restored assets, multi-file order, files-only response, empty response, plugin exception, non-Android no-op, and recovery failure preservation.
- [x] 5.4 Race a retained recovery response for restored draft A against clear/rotation to B after initialization but before recovery acquires arbitration; verify the captured-ID mismatch discards the response with no temp/final file, descriptor, or runtime asset in either session.
- [x] 5.5 When initialization did not restore a valid persisted draft identity or staged reconciliation did not establish trustworthy state, complete recovery best-effort without checkpointing or associating returned files to the new in-memory identity. A draft-load error must preserve existing staged state rather than invoking orphan cleanup, while `Success(null)` retains the intended cleanup behavior. Verify no staged file/descriptor/runtime asset is created and orphan cleanup remains independent of the recovered source. Inspect serialized candidate-processing `Result.error` values and record them through the recovery diagnostic path without turning recovery into a user-visible failure.

## 6. Persistent Removal and Safe Draft Cleanup

- [x] 6.1 At Command action entry, synchronously resolve the requested index to a captured `(clientSubmissionId, digest)` before waiting for lifecycle arbitration; inside the boundary reject identity/digest mismatch as stale work, persist removal, then remove the matching digest rather than whichever asset later occupies the index. Retain current immediately-invalid-index/error reporting and verify descriptor/file/runtime deletion, other-asset order, already-missing-file success, re-addition, repository failure preservation, Task 3.6 races, and a queued draft-A removal after rotation cannot delete from draft B.
- [x] 6.2 Extend `_clear()` inside lifecycle arbitration without regressing Subplan 1: capture the old identity, await `ContentSubmissionDraftRepository.clearDraft()` before destructive staged cleanup, and on draft-clear error preserve form, checkpoint baseline, persisted-draft state, identity, runtime assets, descriptors, and files. Verify pending and failed clear branches retain the complete recoverable session and do not call staged cleanup.
- [x] 6.3 After draft clear succeeds, call idempotent `clearSession(oldIdentity)` before exposing a fresh draft, clear runtime/transient state, rotate identity, and mark the new identity non-durable. On staged-cleanup failure, the staged repository must emit one path-free diagnostic with safe operation/error-category metadata and no raw filesystem exception or stack trace, then finish the now-irreversible rotation, return discard success, and leave residue foreign for later orphan reconciliation; verify normal cleanup removes all old state, a controlled cleanup failure cannot attach residue to the new identity, and a recognizable private path is absent from every logger payload.

- [x] 6.4 Keep `clearSession(clientSubmissionId)` public on the staged repository as the concrete finalization seam and document current/future callers accurately: existing `ContentSubmissionProgressScreen` already invokes `clear` when retiring a completed submission, so it reuses staged cleanup without a new `_submit()` success hook; a later hardening subplan may refine that route-level finalization contract. Verify `_submit()` itself never calls cleanup and no speculative finalization API is added.

## 7. Remote-Failure Preservation and Staged Retry Source

- [x] 7.1 Minimally extend `FakeContentSubmissionRepository` in `test/support/fake_repositories.dart` to capture every `File` passed to `uploadImageTask` and allow queued/gated upload results without changing the production repository API; verify existing fake consumers still compile and no Cloudinary delete/rollback fake is added.
- [x] 7.2 Extend the existing `CloudinaryUploadClientImpl` integration-style tests with its fake preparation client/server and a real extensionless, digest-basename staged file; require the actual multipart client to send `application/octet-stream`, the digest-only filename, and unchanged bytes and to accept the normal successful Cloudinary-shaped response. If this client-boundary test fails, stop and update the OpenSpec rather than improvising an extension field/convention.
- [x] 7.3 Add a submit-source regression using a real picker/source file: stage it, assert the runtime path is under the configured staging root, delete the original source, execute valid submission, and assert the upload fake receives/reads the staged file with the expected bytes rather than the source path.
- [x] 7.4 Add controlled Cloudinary-first-upload and partial-sequential-upload failure tests proving all local descriptors/files/runtime paths remain unchanged, then retry and assert the same staged paths are passed again in existing sequential order; do not alter Cloudinary SHA-256 preparation, cancellation, or rollback behavior.
- [x] 7.5 Add a final `ContentSubmissionRepository.upload` failure/retry test proving staged state survives after all image uploads and retry still reads the same staged files. Assert this change introduces no staged cleanup on any remote error and no eager Cloudinary deletion or backend/API call.

## 8. Tests, Validation, and Scope Review

- [x] 8.1 Run the new real-store/filesystem repository suite plus focused model/mapper tests and require all staging, ownership, relative-path/symlink containment, source/temp/final digest and size integrity, temp/final ordering, deduplication, deterministic duplicate-row winner, explicit descriptor ID ordering, active reconciliation, content/name/size mismatch cleanup, stale-descriptor and descriptor-less-final cleanup, malformed/foreign/no-active orphan cleanup, removal, and idempotent cleanup cases to pass without ObjectBox/Box/Query fakes or per-operation production hooks.
- [x] 8.2 Run `flutter test test/ui/content_submission/view_models/content_submission_view_model_test.dart test/ui/content_submission/widgets/content_submission_asset_list_test.dart test/ui/content_submission/widgets/content_submission_screen_test.dart test/ui/content_submission/widgets/content_submission_progress_screen_test.dart test/routing/content_submission_restoration_test.dart test/data/services/api/cloudinary/cloudinary_upload_client_impl_test.dart` plus the new staged-repository tests, and require all unavailable-state submission blocking, disposal races, initialization/checkpoint races, cross-Command ordering, stale picker/recovery/removal, coordinated restoration, production-order Android recovery, restored-capacity recovery, persistent removal/clear, extensionless client upload, and staged-source retry cases to pass; then run `flutter test` and require the full suite to pass.
- [x] 8.3 Run `dart format --output=none --set-exit-if-changed` over authored/touched Dart source and tests excluding generator-owned Dart, then targeted `flutter analyze` over all authored/touched files. Require zero new or in-scope diagnostics relative to the known two positional-boolean infos in `ContentSubmissionViewModel` and two in `test/support/fake_repositories.dart`; do not broaden work into those pre-existing lints.
- [x] 8.4 Run repository-wide `flutter analyze` and compare with the planning baseline of 189 pre-existing diagnostics recorded at `60f0f9e`: require no new/in-scope diagnostics, but do not require global exit zero or perform unrelated lint cleanup. Verify generated Dart through successful code generation, model-ID/UID preservation, and real-store tests rather than manual edits or generator lint suppression.
- [x] 8.5 Inspect `git status --short`, `git diff --stat`, `git diff`, and `git diff --check`; require changes to stay within the Content Submission staged model/repository/entity/mapper, ViewModel and minimal asset UI if needed, Provider DI, generated ObjectBox pair, shared test support/tests, and these OpenSpec artifacts. Reject new packages, a second session ID, `position`, bytes in ObjectBox, generic media/locking architecture, Admin/progress-route work, Cloudinary semantic changes, or any Supabase/migration/RPC/RLS/Edge Function diff.
- [x] 8.6 Re-read `proposal.md`, the capability spec, `design.md`, and this checklist against the implementation, run `openspec validate harden-content-submission-local-asset-staging --strict --no-interactive`, and require an independent semantic review of lifecycle, persistence, path-containment, unavailable-state submission blocking, disposal behavior, and cleanup. Handoff is complete only when the draft and attachments restore as one session, every runtime/upload consumer uses staged files, stale asynchronous work cannot mutate a rotated or disposed session, all objective gates pass, and the result is reported as Subplan 2 rather than whole-feature release readiness.

## 9. Adversarial Corrective Pass

- [x] 9.1 Distinguish successful draft absence from draft-load failure during initialization. Preserve existing staged descriptors/files and block fresh-identity checkpoint/picker writes while persisted ownership is unknown; retain `Success(null)` orphan cleanup, preserve persistence-first explicit clear behavior, and add real ObjectBox/filesystem regressions for preservation, blocked add/checkpoint, Android lost-data non-association, and authoritative absence.
- [x] 9.2 Sanitize best-effort staged-cleanup diagnostics at the staged repository boundary: emit one useful cleanup event with safe metadata but never forward a raw `FileSystemException`, local path, or path-bearing stack trace. Add a controlled private-path regression covering event data, error, stack trace, and extra payload.
- [x] 9.3 Inspect serialized Android recovered-candidate processing results. Record `Result.error` through the existing recovery diagnostic path without throwing or changing Command UX, and extend the restored-state failure regression to prove valid state remains unchanged and the diagnostic is emitted.
- [x] 9.4 After a draft-load error, keep staged ownership quarantined until persistence-first clear succeeds; then use the existing null-active reconciliation primitive to clean all orphaned staged state before one fresh rotation. Preserve state on clear failure, keep global cleanup best-effort and path-safe, and add real ObjectBox/filesystem regressions for successful cleanup, failed clear preservation, and sanitized global-cleanup failure logging.

## 10. Bounded Maintainability Simplification

- [x] 10.1 Remove staged-repository per-operation production overrides while retaining only `getSupportDirectory`; rewrite retained repository regressions to use real temporary filesystem/ObjectBox state and delete tests that exist solely for operation-hook behavior or hostile intra-operation TOCTOU replacement.
- [x] 10.2 Simplify acquisition, reconciliation, and path resolution so acquisition verifies the completed temporary copy once, each descriptor-backed final is fully validated once during reconciliation, descriptor-less finals are deleted as orphan state without reconstruction/order queries, and resolution performs only structural safety checks; verify retained staging, restoration, corruption, orphan, duplicate, and no-follow-link tests.
- [x] 10.3 Simplify `ContentSubmissionViewModel` lifecycle state by removing the separate initialization barrier and `_hasDurableCheckpoint`, awaiting the memoized initialization future directly, deriving durability from `unknown`/`absent`/`restored`, and checking disposal only at operation entry/publication boundaries; verify retained initialization, checkpoint, ownership, recovery, removal, clear, submission-blocking, and retry regressions.
- [x] 10.4 Remove obsolete reconstruction, per-operation-hook, hostile TOCTOU-window, repeated-validation-order, and per-await-disposal tests while preserving all high-value Subplan-2 coverage; format touched Dart and verify focused repository and Content Submission suites.
- [x] 10.5 Run the full relevant and repository test suites, `flutter analyze` against the existing baseline, generated ObjectBox validity checks, diff/scope review, and `openspec validate harden-content-submission-local-asset-staging --strict --no-interactive`; record production LOC/concept/hash-pass deltas and report Subplan 2 only.
