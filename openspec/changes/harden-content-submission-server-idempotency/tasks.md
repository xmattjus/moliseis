## 1. Lock the verified baseline and implementation prerequisites

- [ ] 1.1 Run `git status --short`, `git rev-parse HEAD`, and `git merge-base --is-ancestor 366bda6f294ee219a88106a700208b4708e21f48 HEAD`; proceed only from the exact commit or a reviewed descendant preserving all four completed Content Submission hardening changes, and record unrelated work without modifying it.
- [ ] 1.2 Verify that every in-repository production caller targeted by this change sends canonical `client_submission_id` and that the current Flutter repository contract accepts both an HTTP 201 created acknowledgement and an HTTP 200 replay acknowledgement while preserving `Result<void>`. If either repository contract is false, stop implementation and reconcile the OpenSpec before writing production code; do not use the compatibility state of already-released historical clients as an implementation/apply gate.
- [ ] 1.3 Run strict validation for the four predecessor changes, the Subplan 5 change, and all main specs; require zero validation errors, inspect the synchronized cumulative requirements, and record only the existing archive-only informational warnings.
- [ ] 1.4 Record pre-change results for `deno test supabase/functions/submit-content/submission_validation_test.ts`, `flutter test test/data/repositories/content_submission_repository_impl_test.dart`, `flutter test test/ui/content_submission/view_models/content_submission_view_model_test.dart`, the affected Content Submission form/progress/routing/restoration tests, complete `flutter test`, and repository-wide `flutter analyze`; compare final verification to this checkout rather than historical counts.

## 2. Add the durable ownership key and transaction boundary

- [ ] 2.1 Create one new forward-only migration with `supabase migration new harden_content_submission_server_idempotency` after `20260830144634_enforce_category_before_publication.sql`; add nullable `public.content_submissions.client_submission_id uuid`, a non-null UUID-v4 check matching the canonical client bits, and user-scoped uniqueness on `(user_id, client_submission_id)`. Run `supabase db reset --local` and verify legacy/Admin rows may remain null while duplicate non-null keys for one user are rejected.
- [ ] 2.2 In that migration add `public.submit_content(...) returns table(outcome text, submission_id bigint)` as a `security invoker` function with explicitly qualified relations; explicitly reject null authenticated user/client identity, accept every validated public content field plus JSON assets as named parameters, and verify direct SQL creation with zero or up to five assets returns exactly one `created` row with a positive ID.
- [ ] 2.3 Capture one transaction timestamp, return a committed key as `replayed` before changing quota, otherwise insert the missing `submission_rate_limits` row with count zero via `INSERT ... ON CONFLICT DO NOTHING`, lock it `FOR UPDATE`, and recheck the key under the lock. Apply the existing fixed-window rule exactly—active only when `window_started_at` is later than transaction time minus 24 hours, reset at or before the boundary—and verify a new key at count five returns `rate_limited` without a content row.
- [ ] 2.4 For a still-new allowed key, insert the submission while mapping a null validated category to the existing `unknown` database value and preserving every other nullable field, reuse `public.add_submission_assets` for the maximum-five invariant, require the expected asset outcomes to be `created`, and update quota in the same transaction; verify any raised/invalid asset operation rolls back submission, assets, and quota together.
- [ ] 2.5 Revoke execute on the new RPC from `public`, `anon`, and `authenticated`, grant it only to `service_role`, and verify existing table RLS/policies and existing `add_submission_assets`/promotion grants are unchanged.
- [ ] 2.6 Regenerate `supabase/functions/_shared/database.types.ts` from the reset local schema using the stdout of `supabase gen types typescript --local`; verify the diff contains only the new nullable table column/check-compatible shapes and `submit_content` argument/result types, with no manual generated-type edits or unrelated schema churn.

## 3. Prove database idempotency, concurrency, and rollback

- [ ] 3.1 Add `supabase/tests/submission_idempotency_db_test.ts` and a runner matching the existing local-host-only `SUPABASE_DB_URL` safety pattern; verify the runner refuses non-local URLs and works after `supabase start`/`supabase db reset`.
- [ ] 3.2 Add first-commit tests with no assets, with valid assets, and with null category; assert exactly one content row, the expected asset rows, one quota increment, persisted authenticated user/key, `unknown` for null category, and `created` with the same positive ID.
- [ ] 3.3 Add sequential same-user/same-key replay tests for identical and otherwise-valid changed bodies/assets; assert `replayed` returns the original ID, row/assets remain the first committed values, and quota is neither created, reset, nor incremented by replay.
- [ ] 3.4 Add replay-after-moderation coverage; change the committed row to accepted or rejected, replay its key, and verify the original ID/status/handling/publication state remain unchanged.
- [ ] 3.5 Add a controlled two-session same-user/same-key race with different valid bodies/assets using the repository's existing `application_name`, lock-wait, and statement-timeout helpers; assert one `created`, one `replayed`, one first-commit-wins row/asset set, one quota increment, and the same ID from both calls.
- [ ] 3.6 Add a same-user/different-key race from four used slots; assert at most one `created`, the other `rate_limited`, and a maximum committed count of five, then prove replay of an existing key still succeeds at the limit without incrementing quota.
- [ ] 3.7 Add cross-user and privilege tests; assert the same key creates independent rows for different authenticated owners, while `anon` and `authenticated` direct RPC execution fail with no state change and `service_role` succeeds.
- [ ] 3.8 Add transaction-failure tests using malformed/non-array asset JSON or another deterministic invariant failure after creation begins; snapshot quota/content/assets before the call and assert the PostgreSQL error leaves every snapshot unchanged.
- [ ] 3.9 Add fixed-window boundary coverage using controlled database timestamps: verify a start strictly newer than 24 hours retains its count, a start exactly 24 hours old or older resets for a new key, and replay returns without resetting even an expired quota row.

## 4. Consume and validate the identity at the Edge boundary

- [ ] 4.1 Extend `ValidatedContentSubmission` and `parseContentSubmission` with required `client_submission_id`, using the exact canonical lowercase UUID-v4 version/variant shape enforced by `ContentSubmissionDraft`; verify the parsed value is preserved unchanged.
- [ ] 4.2 Add the canonical identity to the shared valid submission fixture, replace the old forward-compatibility case with accepted canonical coverage, and add missing, null, uppercase, wrong-version, wrong-variant, malformed, and extra-text rejection cases; run the complete parser suite and verify no prior validation coverage is weakened.
- [ ] 4.3 Add one focused public-submission store boundary following the existing Edge store pattern; have its production implementation call only `submit_content` through `SupabaseClient<Database>`, pass the server-authenticated user ID plus the exact parsed allowlist—including null category unchanged—and validate exactly one known outcome row before returning it.
- [ ] 4.4 Add store tests or handler fake captures that verify exact RPC name/arguments, null/category/date/Delta preservation, asset order, RPC error propagation, and rejection of null, empty, multiple, unknown, or non-positive-ID result shapes without logging request payloads.

## 5. Replace split Edge writes with the atomic result mapping

- [ ] 5.1 Refactor `submit-content/index.ts` only enough to expose a testable handler factory with injected authentication and store construction, matching `admin-content-submissions`/`prepare-cloudinary-upload`; verify OPTIONS, method rejection, missing/invalid auth, oversized/malformed JSON, and validation failure return before store construction.
- [ ] 5.2 Remove the Edge Function's separate rate-limit read/upsert, content insert, and asset RPC sequence and delegate once to the new store/RPC after successful authentication and parsing; verify each accepted request causes exactly one persistence call.
- [ ] 5.3 Map `created` with a positive ID to HTTP 201 `{submission_id, replayed: false}`, `replayed` with the same contract to HTTP 200 `{submission_id, replayed: true}`, and `rate_limited` to HTTP 429 with code `RATE_LIMIT_EXCEEDED` and message `Maximum 5 submissions per 24 hours exceeded`; verify all responses retain the repository's CORS/JSON behavior.
- [ ] 5.4 Map RPC transport/database failures and malformed or unknown outcomes to stable 500 responses, log only safe operation/outcome metadata, and verify tests contain no authored fields, contact data, bearer token, client identity, database details, or local paths in responses/logs.
- [ ] 5.5 Add `supabase/functions/submit-content/index_test.ts` with fake authentication/store dependencies covering creation, replay, rate limit, RPC failure, malformed outcome, auth ordering, parser ordering, and exactly-once store invocation; include an already-known identity with another malformed field and verify validation rejects it before store construction, then run the handler and parser suites together.

## 6. Preserve the Subplan 4 Flutter contract

- [ ] 6.1 Extend `content_submission_repository_impl_test.dart` so a 200 response containing positive `submission_id` plus `replayed: true` returns `Success<void>` and performs one request with the unchanged `client_submission_id`; verify malformed acknowledgements and Function/transport error normalization remain unchanged.
- [ ] 6.2 Perform a repository-wide reference check for `client_submission_id`, `submit_content`, and `submission_id`; verify no Flutter production model exposes the backend ID, no repository/Command/Provider signature changes, and no ViewModel, route, progress, ObjectBox, staged-asset, or Cloudinary production edit is required.
- [ ] 6.3 Rerun the complete `ContentSubmissionViewModel` suite and affected form/progress/routing/restoration suites; verify checkpoint-before-remote, immutable A payload, ownership release, clear rejection, retry identity, finalization-only retry, one identity rotation, and successful visible form reset still pass without weakening assertions.

## 7. Synchronize specifications and complete verification

- [ ] 7.1 Re-read the final implementation against every ADDED/MODIFIED/REMOVED requirement, update planning artifacts only if intended behavior changed, and synchronize the four affected main specs without altering unrelated predecessor requirements.
- [ ] 7.2 Run `deno fmt` on intentionally changed Deno files, followed by `deno fmt --check`, `deno check` on the changed production submit-content entry point, and the focused submit-content Deno tests; require zero failures and no unrelated formatting churn.
- [ ] 7.3 Reset/start the disposable local Supabase stack, run `supabase db lint --local --fail-on error` and `supabase db advisors --local --type all --fail-on error`, then run the new submission-idempotency database runner plus the existing submission-asset and promotion runners; require no new advisor finding and require sequential, boundary, concurrent, privilege, rollback, asset, and promotion invariants all to pass together.
- [ ] 7.4 Run Dart formatting on any intentionally changed authored Dart test, the focused repository/ViewModel/widget/routing/restoration tests, complete `flutter test`, targeted analysis for touched Dart, and repository-wide `flutter analyze`; require no new in-scope diagnostic and no regression from Task 1.4.
- [ ] 7.5 Run `openspec validate harden-content-submission-server-idempotency --strict --no-interactive`, validate all four predecessor changes, run `openspec validate --all --strict --no-interactive`, and run `git diff --check`; inspect untracked migration whitespace separately if needed and require zero validation errors.
- [ ] 7.6 Inspect the final diff and repository-wide references; verify scope is limited to the new migration/RPC, generated Supabase types, submit-content parser/store/handler, focused DB/Edge/Flutter tests, and OpenSpec artifacts, with no package, RLS relaxation, Admin/promotion redesign, ObjectBox, broad Flutter, Cloudinary rollback/deletion, parallel upload, background work, or secret/PII logging change.
- [ ] 7.7 Before deploying a `submit-content` Edge Function that makes `client_submission_id` mandatory, confirm from release/support policy that every still-supported deployed client able to call the endpoint sends the canonical field. If any supported deployed client does not send it, do not deploy the strict Edge Function; implementation and local verification may still complete while rollout waits until a compatible client is the minimum supported production version. If simultaneous support later proves necessary, create a separately reviewed OpenSpec rollout change defining temporary backward-compatibility semantics and tests rather than adding an optional-key or legacy non-idempotent branch to this change.
- [ ] 7.8 Report implementation completion only as **Content Submission Hardening Subplan 5 complete: server-side idempotency and atomic acknowledgement recovery**, and report strict Edge deployment readiness separately according to Task 7.7; explicitly retain Cloudinary rollback/deletion, persistent background submission, multi-draft support, and best-effort staged-cleanup propagation as out of scope rather than declaring the whole feature release-proof.
