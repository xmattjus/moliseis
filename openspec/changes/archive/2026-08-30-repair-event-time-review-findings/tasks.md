## 1. Preserve the Patch Boundary and Canonical Preconditions

- [x] 1.1 Re-inventory `git status --short`, cached/uncached diffs, and all user-owned dirty paths before editing; record baseline `9027a7b1f5d8c22a24736b1506c538c5e3471f98` and verify no unrelated hunk is discarded, overwritten, or staged.
- [x] 1.2 Keep `timezone: ^0.11.1` as a direct dependency in `pubspec.yaml`, resolve packages from the manifest, and verify the dependency is available without relying on an old `.dart_tool/package_config.json`; preserve the unrelated `version: 2.5.0+105` hunk separately for final selective staging.
- [x] 1.3 Treat the corrected backend constraints and stored data as the required execution precondition: persisted event starts are valid and required, ends are optional, and a present end is not before its start; if implementation evidence materially contradicts that precondition, stop and report it rather than adding a compatibility parser, fallback, coercion, normalization, repair, or migration.

## 2. Repair Canonical Persistence and Remove Legacy-Only Date Paths

- [x] 2.1 Give `EventEntity.copyWith.endDate` `_unset`/value/explicit-null semantics and add focused coverage in `test/data/mappers/event_entity_mapper_test.dart` and `test/data/mappers/event_dto_mapper_test.dart`; run both files and verify omission preserves an end while an authoritative remote null clears it.
- [x] 2.2 Replace the missing-start Unix-epoch substitution in `EventEntityMapper` with an explicit persisted-data contract failure, retain canonical UTC conversion, and update focused mapper tests to prove valid required-start/optional-end rows map correctly and a missing persisted start is rejected rather than coerced.
- [x] 2.3 Remove `EventDateDraft` end-only legacy representation, legacy-only predicates/projections, and the corresponding `ContentSubmissionDraftMapper` hydration branch; remove tests whose sole purpose is preserving end-only or malformed historical rows, then run the domain and draft-mapper suites and verify disabled mode, a current unresolved start awaiting its clock, canonical start-only/ranged drafts, and valid current-edit end repair remain covered.

## 3. Complete Canonical Editor Boundaries

- [x] 3.1 Make date/time picker and shared-field callback values non-nullable, remove the six public/admin nullable-`DateTime` adapters and `ContentSubmissionDraft.startDate/endDate` compatibility projections, and migrate affected tests to semantic APIs/`eventDates`; verify repository-wide searches find no removed adapter calls, compatibility-getter consumers, end-only legacy symbols, or malformed-picker accommodations, then run all modified public/admin model, ViewModel, and screen/widget tests using canonical picker bounds only.
- [x] 3.2 In the admin promotion command, run the existing moderation guard before any target-specific temporal validation. After that guard passes for `AdminPromotionTarget.event`, derive a non-mutating event-enabled validation projection from the loaded temporal values and route it through the existing persistence-validation path used by save; do not duplicate temporal readiness rules, assign the projection back to the editor, mark the editor dirty, or change dirty-state semantics. Update the admin ViewModel tests to: (a) normally load a clean pending submission with no event dates and request event promotion, verifying `EventTimeIssue.missingStartDate`, `Result.error`, and no repository promotion; (b) verify an ineligible editor with the same missing temporal values is rejected by the moderation guard before target validation publishes an issue; and (c) retain valid event promotion with explicit persistence-ready dates and place promotion without event dates. Do not use malformed persisted dates, legacy compatibility behavior, unsupported state manipulation, or an artificial save of an invalid event draft. Run `test/ui/admin/submissions/view_models/admin_submission_editor_view_model_test.dart` and require all promotion and moderation-ordering cases to pass.

## 4. Make Selected-Day Loading Coherent

- [x] 4.1 Extend `test/support/fake_repositories.dart` additively with the smallest pending current-year/date results needed to control completion order, preserving all existing defaults; verify current fake consumers still compile and focused event tests can independently complete the two requests.
- [x] 4.2 Tie `EventViewModel` selected-day cache validity to a yearly-data revision, recompute on every successful yearly replacement, invalidate empty projections for a future genuine fallback, and prevent an older in-flight date result from overwriting newer yearly state; run `test/ui/event/view_models/event_view_model_test.dart`, `test/ui/event/widgets/components/events_calendar_test.dart`, and `test/ui/event/widgets/events_screen_test.dart` with cases for date-first initial load, stale fallback completion, same-day reload addition/replacement/removal, and retry after failure.
- [x] 4.3 Remove `EventsCalendar.calendarBuilder`/`EventsCalendarBuilder`, test `calendarDateBounds` directly with canonical inputs, and remove only duplicated pure `isEventOnDay` cases after retaining unique Rome rollover and widget-wiring coverage in the owning suites; run both event ViewModel and calendar widget test files and verify production rendering still uses the real calendar path.

## 5. Remove Wall-Clock Dependence from Repository Proof

- [x] 5.1 Replace current-year/today `DateTime.now()` fixtures in `test/data/repositories/event_repository_impl_test.dart` with fixed UTC clocks injected through the existing `nowUtc` seam and derive each temporal fixture from that same scenario; run the file against the real temporary ObjectBox store and verify current-year/day/upcoming expectations are machine-timezone independent.
- [x] 5.2 Apply the same fixed-clock pattern to every current-year search path in `test/data/repositories/search_repository_impl_test.dart`, retaining non-UTC values only for explicit normalization cases; run the file and verify direct, category, coordinate, city, and query paths share deterministic Rome-year expectations.

## 6. Objective Verification and Staged-Patch Proof

- [x] 6.1 Format every authored Dart source and test changed by this repair, then run targeted analyzer invocation(s) covering every touched authored Dart path; verify formatting exits zero and targeted analysis reports zero diagnostics, without editing generator-owned Dart or unrelated baseline lint.
- [x] 6.2 Run the focused Flutter regression set from Tasks 2–5 together, including mapper contract-failure/nullability, domain-policy, public/admin editor, canonical picker, event cache/screen, and real-ObjectBox repository suites; require every focused test to pass and verify no test exists solely to preserve malformed historical date behavior.
- [x] 6.3 Run repository-wide `flutter test` and `flutter analyze`; require all tests to pass, no analyzer error/warning, no diagnostic in a repair-touched authored path, and no new or worsened out-of-scope informational diagnostic relative to the established 20-info baseline (require analyzer exit zero if the independently rechecked baseline is clean).
- [x] 6.4 Start the established local Supabase stack if necessary and run `supabase/tests/run_submission_promotion_db_test.sh`; require exit zero so canonical event readiness, service-role-only execution, transaction locking, and microsecond copying remain intact, and stop rather than waive this gate if the local database proof cannot run.
- [x] 6.5 Verify repository-wide searches and the final diff contain no newly introduced or retained in-scope compatibility parser, fallback, defensive coercion, normalization, migration path, end-only legacy representation, or inverted-persisted-range picker support; separately confirm valid disabled, unresolved-current-start, and optional-end behavior remains covered.
- [x] 6.6 Selectively update the index with every intended implementation/test repair and only the `timezone` dependency hunk from `pubspec.yaml`; verify `git diff --cached --check` passes, the cached manifest diff contains `timezone` but not the version bump, generated ObjectBox files have no new drift, and unrelated dirty/untracked work remains preserved.
- [x] 6.7 Reconstruct the exact cached event-time patch on baseline `9027a7b1f5d8c22a24736b1506c538c5e3471f98` in an otherwise clean temporary worktree and prove it matches the cached patch, passes diff checks, and contains only the intended `timezone` declaration hunk rather than the unrelated version bump. Do not copy an ignored local `pubspec.lock` or package configuration into that worktree. Run fresh `flutter pub get` only to prove the declared dependency resolves; do not require repository-wide `flutter test` against newly resolved unrelated/transitive dependency versions. The focused and primary-worktree full Flutter gates in Tasks 6.2–6.3 remain authoritative; the observed fresh-resolution failure in `test/data/repositories/admin_content_submission_repository_impl_test.dart` is not an event-time regression and must not be repaired or waived in this change. Record the absence of a tracked application lockfile as a separate reproducibility follow-up, then finish with `openspec validate repair-event-time-review-findings --strict --no-interactive` and stop handoff on any mandatory scoped-proof failure.
