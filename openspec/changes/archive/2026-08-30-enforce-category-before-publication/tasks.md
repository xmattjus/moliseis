## 1. Preserve Pre-Publication Category Semantics

- [x] 1.1 Keep all `supabase/functions/submit-content/` production code unchanged. Where focused coverage is absent, extend only `submission_validation_test.ts` to prove an omitted/null category normalizes to null for the existing handler/database-default path; run `deno test supabase/functions/submit-content/submission_validation_test.ts`, confirm `index.ts` still omits the insert column for null, confirm the schema default remains `unknown`, and verify the implementation diff adds no explicit-public-`unknown` acceptance.
- [x] 1.2 Leave `supabase/functions/admin-content-submissions/admin_submission_validation.ts` unchanged and retain its all-category create/update coverage; run `deno test supabase/functions/admin-content-submissions/admin_submission_validation_test.ts` and require explicit `unknown` create/update parsing to remain valid.

## 2. Extend the Admin Promotion API Contract

- [x] 2.1 Add `{ outcome: "category_required" }` to `PromoteStoreResult` and the explicit null-payload domain-failure branch in `admin_submission_store.ts`; extend the table-driven store tests and verify recognized null payloads parse while populated/omitted payload fields and unknown outcomes still fail closed with `deno test supabase/functions/admin-content-submissions/admin_submission_store_test.ts`.
- [x] 2.2 Map `category_required` in `promotionResponse()` to HTTP `422`, code `PROMOTION_CATEGORY_REQUIRED`, and message `The submission requires a category before publication.`; add the tuple to `maps every promotion readiness outcome to a stable error`, retain successful admin create/update fixtures using `category: "unknown"`, and run `deno test supabase/functions/admin-content-submissions/index_test.ts`.

## 3. Enforce Category Readiness in the Database

- [x] 3.1 Create a new timestamped Supabase migration, without editing `20260825170713_promote_content_submission.sql`, that uses `CREATE OR REPLACE FUNCTION` for the exact `public.promote_content_submission(bigint, text, uuid)` contract; preserve `(outcome text, target_type text, entity_id bigint)`, `SECURITY INVOKER`, service-role-only execution, locks, transactionality, and all existing logic, and verify the migration diff contains no enum/default/table-schema or unrelated function changes.
- [x] 3.2 In the replacement function, preserve durable-link `already_promoted` resolution before status/readiness checks and add `(category_required, null, null)` after existing asset validation and immediately before target insertion; verify code review shows no target, media, link, handled/status mutation can precede the new return and no existing readiness check was reordered.
- [x] 3.3 In `supabase/tests/submission_promotion_db_test.ts`, change only `createSubmission()`'s promotion-ready default from `unknown` to a concrete category such as `nature`; retain explicit concrete-category copy assertions and verify the diff does not alter production defaults or unrelated shared fixtures.
- [x] 3.4 Replace `unknown category is a valid publication value` with otherwise-ready place and event failure cases, each with valid coordinates/city, valid event dates when applicable, and at least one valid source asset; snapshot the source row/assets and assert `category_required` with null payload, no target or target media, unchanged category/assets/`handled_by`/`handled_at`/`modified_at`, pending status, and null links, then run `bash supabase/tests/run_submission_promotion_db_test.sh` against the disposable local stack.
- [x] 3.5 Extend an existing idempotency case so an already-linked source has category `unknown` before retry; rerun the database suite and require `already_promoted` with the original target/id and no duplicate target/media. Also require the existing same/conflicting-target retries, concrete place/event category copying, media-parent/source-asset preservation, concurrent row-lock serialization, outer-transaction rollback, and `promotion executes only as service_role` checks to remain green, including `SECURITY INVOKER` and denied `anon`/`authenticated` execution.

## 4. Add Narrow Flutter Publication Readiness

- [x] 4.1 In `AdminSubmissionEditorViewModel`, add a derived `hasPublishableCategory` for non-null/non-unknown category and check it in `_promote()` after `_moderationGuardError()` but before event-target temporal validation and repository access; keep `_save()` null-to-unknown behavior, `_reject()`, event-time validation, and the domain repository contract unchanged.
- [x] 4.2 Extend `admin_submission_editor_view_model_test.dart` to prove unknown/unset promotion returns a local error without a `promote` call, concrete category still delegates, clean unknown still delegates rejection, and create/save still persists unknown; run `flutter test test/ui/admin/submissions/view_models/admin_submission_editor_view_model_test.dart`.
- [x] 4.3 In `admin_submission_editor_screen.dart`, disable only Publish for `!hasPublishableCategory`, show `Seleziona una categoria e salva prima di pubblicare.` for clean pending unknown/unset state, retain dirty-state message precedence and Reject's current predicate, and map `PROMOTION_CATEGORY_REQUIRED` to the same Italian guidance without changing target selection or confirmation flow.
- [x] 4.4 Extend `admin_submission_editor_screen_test.dart` for clean unknown Publish-disabled/Reject-enabled/guidance, concrete selection returning to existing dirty-state behavior, successful save returning to a clean Publish-enabled state, and backend code mapping; run `flutter test test/ui/admin/submissions/widgets/admin_submission_editor_screen_test.dart`.

## 5. Objective Verification and Handoff

- [x] 5.1 Run `deno fmt --check` on every touched TypeScript file and the focused public/admin Deno tests from Tasks 1-2 together; require formatting and all tests to pass with no weakening of malformed-result or unsupported-category coverage.
- [x] 5.2 Run the local promotion database wrapper after applying all migrations to a disposable local Supabase stack; require every place/event, no-side-effect, idempotency, locking, rollback, and service-role assertion to pass, and stop rather than waive the gate if the local database proof is unavailable.
- [x] 5.3 Run `dart format` on the touched authored Dart files, verify formatting with `dart format --output=none --set-exit-if-changed` on those files, run the two focused Flutter test files together, and run targeted `flutter analyze` on the touched ViewModel, screen, and tests; require zero targeted diagnostics and do not edit generated Dart or unrelated lint.
- [x] 5.4 Run repository-wide `flutter test` and `flutter analyze`; require tests to pass and no new/worsened diagnostic relative to the verified 20-information baseline (with zero diagnostics in touched authored paths), without broadening this change into unrelated analyzer cleanup.
- [x] 5.5 Run `openspec validate enforce-category-before-publication --strict --no-interactive` and `git diff --check`; because a new migration is initially untracked, also inspect `git status --short` and run an equivalent `git diff --no-index --check /dev/null <new-migration>` whitespace check (a differences-only exit is expected, whitespace-error output is not). Inspect the final diff to confirm only the planned migration, validators/contracts, focused UI, and tests changed; stop and update these artifacts before continuing if implementation requires any enum/default/schema, repository-protocol, event-time, target-selection, asset, or generic-readiness redesign.
