## 1. Preserve the Follow-up Boundary

- [x] 1.1 Reinspect `git status --short`, `git diff --cached`, and `git diff` before editing; treat the current staged event-time patch as the implementation baseline, record any unrelated unstaged/untracked work, and verify this follow-up will not modify `repair-event-time-review-findings` or absorb its artifacts.
- [x] 1.2 Confirm the current symbols and contracts named in `design.md` still match the checkout; stop and report rather than expanding scope if either finding requires an ObjectBox legacy state/parser, generated-code edit, dependency change, Supabase migration, persisted-row rewrite, database constraint, or promotion/RPC change.

## 2. Harden Shared Picker Presentation

- [x] 2.1 In `lib/ui/content_submission/widgets/content_submission_date_chip.dart`, calculate one date-mode presentation triple that retains the requested lower bound, clamps an out-of-bounds selected date only for the picker initial value, and derives a valid upper bound from that effective initial year; feed the same triple to Material and compact iOS/Cupertino paths without writing it to controlled editor state or changing time-mode behavior. Extend `test/ui/content_submission/widgets/content_submission_date_chip_test.dart` with an explicit cross-year inversion (selected end in December 2026, lower bound in January 2027) and assert actual Material `initialDate >= firstDate` and `lastDate >= firstDate` plus Cupertino `initialDateTime >= minimumDate` and `maximumDate >= minimumDate`; retain already-valid-value coverage, then run `flutter test test/ui/content_submission/widgets/content_submission_date_chip_test.dart` and require all cases to pass without framework assertions.
- [x] 2.2 Add the focused admin controlled-editor regression in `test/ui/admin/submissions/widgets/admin_submission_editor_screen_test.dart` using its existing repository/router harness: load the same cross-year inverted start/end pair, open and cancel the end picker, and prove the exact ViewModel values and clean state are unchanged, no repository update occurs, and existing persistence validation still reports `EventTimeIssue.invalidRange`; also retain or assert a confirmed allowed replacement flows through the normal semantic callback. Run `flutter test test/ui/admin/submissions/widgets/admin_submission_editor_screen_test.dart` and require both Material picker interaction and save blocking/repair behavior to pass without adding a production test seam or duplicate support helper.

## 3. Enforce Gregorian Validity at the Shared Request Boundary

- [x] 3.1 In `supabase/functions/_shared/submission_dates.ts`, recognize only the intended ISO-like lexical contract (`YYYY-MM-DD` and the existing offset-less, `Z`, explicit numeric-offset, and supported fractional-second `YYYY-MM-DD` date-times), reject alternate runtime-parser spellings, validate Gregorian components for every accepted form, and only then reuse existing instant parsing and pairing/ordering. Preserve original strings, nullable behavior, error codes, and `instantKey`/microsecond ordering. Expand `supabase/functions/_shared/submission_dates_test.ts` to prove `2000-02-29`, `2024-02-29`, and `2024-04-30` valid; `1900-02-29`, `2023-02-29`, and `2024-04-31` invalid; and equivalent impossible offset-less, `Z`, explicit-offset, and fractional-second values invalid for both start and end fields. Run `deno test supabase/functions/_shared/submission_dates_test.ts` and require the lexical/calendar matrix plus existing pairing/precision cases to pass.
- [x] 3.2 Extend `supabase/functions/submit-content/submission_validation_test.ts` and `supabase/functions/admin-content-submissions/admin_submission_validation_test.ts` at their actual parse boundaries to reject impossible start/end dates—including representative date-only, `Z`, explicit-offset, and fractional-second variants—with each adapter's existing message for create/update as applicable, and to preserve every intended date-only, offset-less, `Z`, positive/negative numeric-offset, fractional-second, original-string, offset-equivalent, and sub-millisecond ordering case. Run `deno test supabase/functions/submit-content/submission_validation_test.ts supabase/functions/admin-content-submissions/admin_submission_validation_test.ts` and require all adapter regressions to pass without changing adapter request envelopes or persistence code.

## 4. Verify the Focused Change

- [x] 4.1 Run `flutter analyze lib/ui/content_submission/widgets/content_submission_date_chip.dart test/ui/content_submission/widgets/content_submission_date_chip_test.dart test/ui/admin/submissions/widgets/admin_submission_editor_screen_test.dart` and require zero diagnostics in touched/authored Dart; do not require a repository-wide analyzer comparison or unrelated lint cleanup. Then run `flutter test test/ui/content_submission/widgets/content_submission_date_chip_test.dart test/ui/content_submission/widgets/content_submission_fields_test.dart test/ui/content_submission/widgets/content_submission_screen_test.dart test/ui/content_submission/view_models/content_submission_view_model_test.dart test/ui/admin/submissions/widgets/admin_submission_editor_screen_test.dart test/ui/admin/submissions/view_models/admin_submission_editor_view_model_test.dart` and require all shared public/admin picker and validation consumers to pass.
- [x] 4.2 Run `deno fmt --check supabase/functions/_shared/submission_dates.ts supabase/functions/_shared/submission_dates_test.ts supabase/functions/submit-content/submission_validation_test.ts supabase/functions/admin-content-submissions/admin_submission_validation_test.ts`, `deno check supabase/functions/submit-content/index.ts supabase/functions/admin-content-submissions/index.ts`, and the combined three-file Deno test command from Tasks 3.1–3.2; require zero exits and no loss of microsecond-aware ordering or supported valid forms.
- [x] 4.3 Inspect the final follow-up diff and run `git diff --check` plus `git diff --cached --check`; verify only the picker/shared-validator implementation and focused tests (plus these new OpenSpec artifacts) were added relative to the recorded baseline, with no `legacyEndOnly` equivalent, event-mode inference, compatibility hydration/parser, local-data migration, database migration/SQL, generated ObjectBox, dependency, or unrelated drift. Finish with `openspec validate harden-event-time-invalid-input-handling --strict --no-interactive` and stop handoff on any scoped test, analysis, format, type-check, diff, or validation failure.
