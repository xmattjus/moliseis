include: package:lint/strict.yaml

analyzer:
  errors:
    todo: info
  exclude:
    - '**.g.dart'

linter:
  rules:
    - discarded_futures: true
    - unawaited_futures: true

plugins:
  app_lints:
    path: packages/app_lints